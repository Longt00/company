## 企业管理后台系统 - 完整接口文档

## 📋 概述

企业管理后台系统提供完整的公司信息管理、产品管理、新品管理、主要产品管理、内容管理、文件上传等功能。系统采用RESTful API设计，支持JWT认证，提供统一的数据格式和错误处理机制。

**基础URL**: `http://localhost:33380`
**认证方式**: JWT Bearer Token
**数据格式**: JSON
**API版本**: v1.5
**更新日期**: 2025-11-30

---

## 🔐 认证说明

大部分API需要在请求头中携带JWT Token：
```http
Authorization: Bearer <your-jwt-token>
Content-Type: application/json
```

---

## 📊 统一响应格式

所有API返回统一的响应格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "timestamp": "2023-12-01T10:00:00"
}
```

## 📊 数据模型

### User 对象

| 字段名     | 类型   | 说明     | 示例                  |
| ---------- | ------ | -------- | --------------------- |
| id         | Long   | 用户ID   | 1                     |
| username   | String | 用户名   | "admin"               |
| email      | String | 邮箱地址 | "admin@example.com"   |
| phone      | String | 手机号码 | "13800138000"         |
| createTime | String | 创建时间 | "2023-12-01T10:00:00" |

### Company 对象

| 字段名              | 类型    | 说明     | 示例                                 |
| ------------------- | ------- | -------- | ------------------------------------ |
| id                  | Long    | 公司ID   | 1                                    |
| companyName         | String  | 公司名称 | "测试科技有限公司"                   |
| establishDate       | String  | 成立日期 | "2020-01-01"                         |
| registeredCapital   | String  | 注册资本 | "1000万人民币"                       |
| legalRepresentative | String  | 法人代表 | "张三"                               |
| companyAddress      | String  | 公司地址 | "北京市朝阳区xxx街道xxx号"           |
| contactPhone        | String  | 联系电话 | "010-12345678"                       |
| email               | String  | 邮箱地址 | "info@testcompany.com"               |
| businessScope       | String  | 经营范围 | "技术开发、技术咨询、技术服务"       |
| companyProfile      | String  | 公司简介 | "这是一家专注于技术研发的创新型公司" |
| status              | Integer | 状态     | 1                                    |
| createTime          | String  | 创建时间 | "2023-12-01T10:00:00"                |
| updateTime          | String  | 更新时间 | "2023-12-01T10:00:00"                |

### Product 对象（完整42个属性）

| 字段名                 | 类型       | 说明                           | 示例                                |
| ---------------------- | ---------- | ------------------------------ | ----------------------------------- |
| **系统字段**           |            |                                |                                     |
| id                     | Long       | 产品ID                         | 1                                   |
| createTime             | String     | 创建时间（ISO格式）            | "2025-11-14T10:00:00"               |
| updateTime             | String     | 更新时间（ISO格式）            | "2025-11-14T10:00:00"               |
| createdBy              | Long       | 创建者ID                       | 1                                   |
| updatedBy              | Long       | 更新者ID                       | 1                                   |
| **基本信息**           |            |                                |                                     |
| productName            | String     | 产品名称，最大200字符          | "商务休闲牛仔裤"                    |
| productCode            | String     | 产品编码，最大100字符          | "JEANS-001"                         |
| category               | String     | 产品分类，最大100字符          | "服装"                              |
| description            | String     | 产品描述，最大2000字符         | "高品质商务休闲牛仔裤，舒适透气"    |
| specifications         | String     | 产品规格，最大500字符          | "修身版型，经典蓝色"                |
| **媒体资源**           |            |                                |                                     |
| mainImage              | String     | 产品主图URL，最大500字符       | "https://oss.example.com/jeans.jpg" |
| productImages          | String     | 产品图片列表JSON格式           | "[\"img1.jpg\", \"img2.jpg\"]"      |
| videoPath              | String     | 产品视频路径，最大500字符      | "/uploads/jeans-demo.mp4"           |
| **价格库存**           |            |                                |                                     |
| price                  | BigDecimal | 产品价格，不能为负数           | 299.00                              |
| marketPrice            | BigDecimal | 市场价格，不能为负数           | 399.00                              |
| stockQuantity          | Integer    | 库存数量，不能为负数           | 500                                 |
| **服装材质属性**       |            |                                |                                     |
| material               | String     | 材质，最大100字符              | "98%棉 2%氨纶"                      |
| fabricType             | String     | 面料类型，最大100字符          | "牛仔布"                            |
| fabricWeight           | String     | 面料克重，最大50字符           | "320g"                              |
| **服装款式属性**       |            |                                |                                     |
| waistStyle             | String     | 腰型，最大50字符               | "中腰"                              |
| jeansStyle             | String     | 牛仔裤款式，最大50字符         | "直筒"                              |
| patternFit             | String     | 板型，最大50字符               | "修身"                              |
| style                  | String     | 样式，最大100字符              | "商务休闲"                          |
| **工艺制造属性**       |            |                                |                                     |
| craftsmanship          | String     | 工艺，最大200字符              | "水洗做旧工艺"                      |
| weavingMethod          | String     | 编织方法，最大100字符          | "斜纹编织"                          |
| washingProcess         | String     | 洗水工艺，最大100字符          | "石洗酵洗"                          |
| needleDetectionProcess | String     | 检针工艺，最大50字符           | "磁检"                              |
| **图案设计属性**       |            |                                |                                     |
| patternType            | String     | 图案类型，最大100字符          | "纯色"                              |
| printingMethod         | String     | 印花方法，最大100字符          | "无"                                |
| fashionElements        | String     | 流行元素，最大200字符          | "简约经典，金属扣装饰"              |
| logoPosition           | String     | 徽标位置，最大100字符          | "后腰右侧"                          |
| **供应商业属性**       |            |                                |                                     |
| supplyType             | String     | 供应类型，最大50字符           | "现货供应"                          |
| origin                 | String     | 原产地，最大100字符            | "中国"                              |
| fastSampling           | Boolean    | 7天快速打样：false-否 true-是  | true                                |
| **规格包装属性**       |            |                                |                                     |
| model                  | String     | 型号，最大50字符               | "S/M/L/XL/XXL"                      |
| season                 | String     | 季节，最大50字符               | "四季"                              |
| salesUnit              | String     | 销售单位，最大50字符           | "件"                                |
| **其他属性**           |            |                                |                                     |
| tags                   | String[]   | 产品标签列表                   | ["牛仔裤", "商务", "休闲"]          |
| status                 | Integer    | 产品状态：0-下架 1-上架 2-草稿 | 1                                   |
| isFeatured             | Boolean    | 是否推荐：false-否 true-是     | true                                |
| sortOrder              | Integer    | 排序值，不能为负数             | 10                                  |
| seoKeywords            | String     | SEO关键词，最大200字符         | "牛仔裤,商务休闲,修身"              |
| seoDescription         | String     | SEO描述，最大500字符           | "高品质商务休闲牛仔裤"              |

**字段约束说明**：

- **必填字段创建时**：`productName` 为必填
- **数值约束**：`price`、`marketPrice` 不能为负数；`stockQuantity`、`sortOrder` 不能为负数
- **状态值含义**：
  - `status`: 0-下架, 1-上架, 2-草稿
  - `isFeatured`: false-非推荐, true-推荐
  - `fastSampling`: false-不支持7天打样, true-支持7天打样
- **字符串长度限制**：各字段都有最大长度限制，详见说明列
- **特殊格式**：`productImages` 为JSON格式的字符串数组
- **服装专业属性**：包含材质、款式、工艺等21个专业属性字段
- **包装发货信息**：包含销售单位、型号、季节等包装相关属性

### MediaFile 对象

| 字段名       | 类型    | 说明       | 示例                               |
| ------------ | ------- | ---------- | ---------------------------------- |
| id           | Long    | 文件ID     | 1                                  |
| originalName | String  | 原始文件名 | "logo.png"                         |
| fileUrl      | String  | 文件URL    | "https://oss.example.com/logo.png" |
| fileType     | String  | 文件类型   | "image"                            |
| category     | String  | 文件分类   | "logo"                             |
| fileSize     | Long    | 文件大小   | 102400                             |
| width        | Integer | 图片宽度   | 200                                |
| height       | Integer | 图片高度   | 200                                |
| status       | Integer | 文件状态   | 1                                  |
| createTime   | String  | 上传时间   | "2023-12-01T10:00:00"              |

### OperationLog 对象

| 字段名          | 类型   | 说明                                         | 示例                       |
| --------------- | ------ | -------------------------------------------- | -------------------------- |
| id              | Long   | 日志ID                                       | 123                        |
| operationType   | String | 操作类型（UPLOAD/UPDATE/DELETE/CREATE）      | "UPLOAD"                   |
| operationModule | String | 操作模块（IMAGE/VIDEO/PRODUCT/COMPANY/FILE） | "IMAGE"                    |
| operationDesc   | String | 操作描述                                     | "上传了公司Logo: logo.png" |
| targetType      | String | 目标类型                                     | "MediaFile"                |
| targetId        | Long   | 目标ID                                       | 456                        |
| targetName      | String | 目标名称                                     | "logo.png"                 |
| userId          | Long   | 操作用户ID                                   | 1                          |
| username        | String | 操作用户名                                   | "admin"                    |
| ipAddress       | String | IP地址                                       | "192.168.1.100"            |
| userAgent       | String | 浏览器信息                                   | "Mozilla/5.0..."           |
| createTime      | String | 操作时间（ISO格式）                          | "2025-01-29T10:30:00"      |

---

## 🎯 接口使用最佳实践

### 认证管理

- 所有需要认证的接口都需要在请求头中添加 `Authorization: Bearer <token>`
- Token有效期为24小时，过期后需要重新登录获取新Token

### 错误处理

- 所有接口都返回统一的响应格式，包含code、message、data和timestamp字段
- 根据HTTP状态码和响应code来判断请求结果
- 常见错误码：400-参数错误、401-未认证、404-资源不存在、500-服务器错误

### 分页查询

- 分页参数：page从0开始，size默认10，最大100
- 分页响应包含totalElements、totalPages、size、number等信息

### 文件上传

- 图片文件：支持jpg、png、gif等格式，最大5MB
- 视频文件：支持mp4、avi、mov等格式，最大150MB
- 支持批量上传和增强处理功能

---

## 🎯 接口列表

---

## 1. 系统健康检查接口

### 1.1 系统健康检查

**接口地址**: `GET /api/health`

**功能描述**: 检查系统各组件的健康状态

**是否需要认证**: 否

**请求示例**:
```http
GET /api/health
```

```bash
curl -X GET http://localhost:33380/api/health
```

**响应示例**:
```json
{
  "code": 200,
  "message": "系统健康",
  "data": {
    "database": "连接正常",
    "application": "运行正常",
    "oss": "连接正常",
    "jwt": "配置正常"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

---

### 1.2 存活检查

**接口地址**: `GET /api/alive`

**功能描述**: 简单的系统存活检查

**是否需要认证**: 否

**请求示例**:
```http
GET /api/alive
```

```bash
curl -X GET http://localhost:33380/api/alive
```

**响应示例**:
```json
{
  "code": 200,
  "message": "系统存活",
  "data": "OK",
  "timestamp": "2023-12-01T10:00:00"
}
```

---

## 2. 用户认证接口

### 2.1 用户注册

**接口地址**: `POST /api/auth/register`

**功能描述**: 创建新用户账号

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | String | 是 | 用户名，3-20位字符 |
| password | String | 是 | 密码，6-20位字符 |
| email | String | 是 | 邮箱地址 |
| phone | String | 是 | 手机号码 |

**请求示例**:
```http
POST /api/auth/register
Content-Type: application/json

{
  "username": "testuser",
  "password": "password123",
  "email": "test@example.com",
  "phone": "13800138000"
}
```

```bash
curl -X POST http://localhost:33380/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "password123",
    "email": "test@example.com",
    "phone": "13800138000"
  }'
```

**响应示例**:
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com",
    "phone": "13800138000",
    "createTime": "2023-12-01T10:00:00"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

---

### 2.2 用户登录

**接口地址**: `POST /api/auth/login`

**功能描述**: 用户身份验证，获取JWT Token

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | String | 是 | 用户名 |
| password | String | 是 | 密码 |

**请求示例**:
```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "password123"
}
```

```bash
curl -X POST http://localhost:33380/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "password123"
  }'
```

**响应示例**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "testuser",
      "email": "test@example.com"
    },
    "expiresIn": 86400
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 2.3 获取当前用户信息
**请求方式**: `GET`
**接口地址**: `/api/auth/user-info`
**是否需要认证**: 是

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/auth/user-info \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com",
    "phone": "13800138000",
    "createTime": "2023-12-01T10:00:00"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 2.4 修改密码
**请求方式**: `POST`
**接口地址**: `/api/auth/change-password`
**是否需要认证**: 是

**请求参数:**
```json
{
  "oldPassword": "oldpassword123",
  "newPassword": "newpassword123"
}
```

**请求示例:**
```bash
curl -X POST http://localhost:33380/api/auth/change-password \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "oldPassword": "oldpassword123",
    "newPassword": "newpassword123"
  }'
```

### 2.5 检查用户名是否存在
**请求方式**: `GET`
**接口地址**: `/api/auth/check-username`
**是否需要认证**: 否

**请求参数:**
- `username` (String): 用户名

**请求示例:**
```bash
curl -X GET "http://localhost:33380/api/auth/check-username?username=testuser"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "检查完成",
  "data": {
    "exists": true
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 2.6 检查邮箱是否存在
**请求方式**: `GET`
**接口地址**: `/api/auth/check-email`
**是否需要认证**: 否

**请求参数:**
- `email` (String): 邮箱地址

**请求示例:**
```bash
curl -X GET "http://localhost:33380/api/auth/check-email?email=test@example.com"
```

---

## 3. 公司信息管理接口

### 3.1 获取公司基本信息（公开）
**请求方式**: `GET`
**接口地址**: `/api/company/info`
**是否需要认证**: 否

**请求示例:**

```bash
curl -X GET http://localhost:33380/api/company/info
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "companyName": "测试科技有限公司",
    "establishDate": "2020-01-01",
    "registeredCapital": "1000万人民币",
    "legalRepresentative": "张三",
    "companyAddress": "北京市朝阳区xxx街道xxx号",
    "contactPhone": "010-12345678",
    "email": "info@testcompany.com",
    "businessScope": "技术开发、技术咨询、技术服务",
    "companyProfile": "这是一家专注于技术研发的创新型公司",
    "status": 1,
    "createTime": "2023-12-01T10:00:00",
    "updateTime": "2023-12-01T10:00:00"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 3.2 获取公司基本信息（管理）
**请求方式**: `GET`
**接口地址**: `/api/company/admin/info`
**是否需要认证**: 是

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/company/admin/info \
  -H "Authorization: Bearer your-jwt-token"
```

### 3.3 更新公司基本信息
**请求方式**: `PUT`
**接口地址**: `/api/company/admin/info`
**是否需要认证**: 是

**请求参数:**
```json
{
  "companyName": "新科技有限公司",
  "establishDate": "2020-01-01",
  "registeredCapital": "1000万人民币",
  "legalRepresentative": "李四",
  "companyAddress": "北京市海淀区xxx街道xxx号",
  "contactPhone": "010-87654321",
  "email": "contact@newcompany.com",
  "businessScope": "软件开发、系统集成、技术咨询",
  "companyProfile": "这是一家专注于软件开发和系统集成的科技公司",
  "status": 1
}
```

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/company/admin/info \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "companyName": "新科技有限公司",
    "establishDate": "2020-01-01",
    "registeredCapital": "1000万人民币",
    "legalRepresentative": "李四",
    "companyAddress": "北京市海淀区xxx街道xxx号",
    "contactPhone": "010-87654321",
    "email": "contact@newcompany.com",
    "businessScope": "软件开发、系统集成、技术咨询",
    "companyProfile": "这是一家专注于软件开发和系统集成的科技公司",
    "status": 1
  }'
```

### 3.4 修改自身公司信息

**请求方式**: `PUT`
**接口地址**: `/api/company/admin/update`
**是否需要认证**: 是

**功能描述**: 允许用户修改自己公司的信息

**请求参数:**
```json
{
  "companyName": "修改后的公司名称",
  "contactPhone": "010-88888888",
  "email": "updated@company.com"
}
```

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/company/admin/update \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "companyName": "修改后的公司名称",
    "contactPhone": "010-88888888",
    "email": "updated@company.com"
  }'
```

**响应示例:**
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 1,
    "companyName": "修改后的公司名称",
    "contactPhone": "010-88888888",
    "email": "updated@company.com",
    "updateTime": "2025-01-29T10:00:00"
  },
  "timestamp": "2025-01-29T10:00:00"
}
```

### 3.4 部分更新公司信息
**请求方式**: `PATCH`
**接口地址**: `/api/company/admin/info`
**是否需要认证**: 是

**请求参数:**

```json
{
  "companyName": "最新科技有限公司",
  "contactPhone": "010-11111111"
}
```

**请求示例:**

```bash
curl -X PATCH http://localhost:33380/api/company/admin/info \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "companyName": "最新科技有限公司",
    "contactPhone": "010-11111111"
  }'
```

### 3.5 初始化公司信息
**请求方式**: `POST`
**接口地址**: `/api/company/admin/info/init`
**是否需要认证**: 是

**请求参数:**
```json
{
  "companyName": "初始科技有限公司",
  "establishDate": "2020-01-01",
  "registeredCapital": "1000万人民币",
  "legalRepresentative": "王五",
  "companyAddress": "北京市西城区xxx街道xxx号",
  "contactPhone": "010-22222222",
  "email": "info@initialcompany.com",
  "businessScope": "技术开发、技术服务",
  "companyProfile": "新成立的科技公司",
  "status": 1
}
```

### 3.6 检查公司信息是否存在
**请求方式**: `GET`
**接口地址**: `/api/company/admin/info/check`
**是否需要认证**: 是

**请求示例:**

```bash
curl -X GET http://localhost:33380/api/company/admin/info/check \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "检查完成",
  "data": {
    "exists": true,
    "hasBasicInfo": true,
    "hasContactInfo": true,
    "hasQualifications": false
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 3.7 修改WhatsApp联系方式
**请求方式**: `PUT`
**接口地址**: `/api/company/admin/contact/whatsapp`
**是否需要认证**: 是

**请求参数:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| whatsapp | String | 否 | WhatsApp账号 |
| status | Integer | 否 | 状态（0-禁用 1-启用） |

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/company/admin/contact/whatsapp \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "whatsapp": "+8615920419867",
    "status": 1
  }'
```

**响应示例:**
```json
{
  "code": 200,
  "message": "WhatsApp联系方式修改成功",
  "data": {
    "success": true,
    "whatsapp": "+8615920419867",
    "message": "WhatsApp联系方式修改成功",
    "updateTime": "2025-11-24T13:45:00"
  },
  "timestamp": "2025-11-24T13:45:00"
}
```

### 3.8 获取WhatsApp联系方式（公共接口）
**请求方式**: `GET`
**接口地址**: `/api/company/public/contact/whatsapp`
**是否需要认证**: 否

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/company/public/contact/whatsapp
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取WhatsApp联系方式成功",
  "data": {
    "whatsapp": "+8615920419867",
    "hasWhatsApp": true
  },
  "timestamp": "2025-11-24T13:45:00"
}
```

### 3.9 更换公司Logo
**请求方式**: `PUT`
**接口地址**: `/api/company/admin/logo`
**是否需要认证**: 是

**请求参数:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| logoUrl | String | 是 | Logo文件URL |

**请求示例:**
```bash
curl -X PUT "http://localhost:33380/api/company/admin/logo?logoUrl=https://example.com/new-logo.png" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "公司Logo更换成功",
  "data": {
    "success": true,
    "logoUrl": "https://example.com/new-logo.png",
    "message": "公司Logo更换成功",
    "updateTime": "2025-11-24T13:45:00"
  },
  "timestamp": "2025-11-24T13:45:00"
}
```

### 3.10 上传并更换公司Logo（一体化接口）
**请求方式**: `POST`
**接口地址**: `/api/company/admin/logo/upload`
**是否需要认证**: 是

**请求参数:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | MultipartFile | 是 | Logo图片文件 |

**支持的文件格式**: jpg、jpeg、png、gif、webp、svg
**文件大小限制**: 5MB

**请求示例:**
```bash
curl -X POST http://localhost:33380/api/company/admin/logo/upload \
  -H "Authorization: Bearer your-jwt-token" \
  -F "file=@/path/to/logo.png"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "Logo上传并更换成功",
  "data": {
    "success": true,
    "logoUrl": "http://localhost:33380/api/files/logo/20251124134500_logo.png",
    "fileName": "logo.png",
    "fileSize": 245760,
    "message": "Logo上传并更换成功",
    "updateTime": "2025-11-24T13:45:00"
  },
  "timestamp": "2025-11-24T13:45:00"
}
```

### 3.11 获取公司Logo（公共接口）
**请求方式**: `GET`
**接口地址**: `/api/company/public/logo`
**是否需要认证**: 否

**功能描述**: 获取公司当前设置的Logo信息，供前端展示使用

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/company/public/logo
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取Logo成功",
  "data": {
    "logoUrl": "https://example.com/company-logo.png",
    "hasLogo": true,
    "logoInfo": {
      "fileName": "company-logo.png",
      "fileSize": 245760,
      "uploadTime": "2025-11-24T13:45:00"
    }
  },
  "timestamp": "2025-11-24T13:45:00"
}
```

**无Logo时的响应示例:**
```json
{
  "code": 200,
  "message": "获取Logo成功",
  "data": {
    "logoUrl": null,
    "hasLogo": false,
    "logoInfo": null
  },
  "timestamp": "2025-11-24T13:45:00"
}
```

**使用说明:**
- 该接口无需认证，可用于前端页面直接展示公司Logo
- 当没有设置Logo时，`logoUrl`为`null`，`hasLogo`为`false`
- 前端可根据`hasLogo`字段判断是否显示默认Logo

---

## 4. 公司公共信息接口

### 4.1 获取公司联系方式
**请求方式**: `GET`
**接口地址**: `/api/public/company/contact`
**是否需要认证**: 否

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/public/company/contact
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "contactPhone": "010-12345678",
    "email": "info@testcompany.com",
    "companyAddress": "北京市朝阳区xxx街道xxx号",
    "website": "https://www.testcompany.com"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 4.2 获取所有有效资质文件
**请求方式**: `GET`
**接口地址**: `/api/public/company/qualifications`
**是否需要认证**: 否

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/public/company/qualifications
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "name": "营业执照",
      "fileUrl": "https://oss.example.com/qualifications/license.jpg",
      "type": "BUSINESS_LICENSE",
      "status": 1,
      "createTime": "2023-12-01T10:00:00"
    },
    {
      "id": 2,
      "name": "ISO9001认证证书",
      "fileUrl": "https://oss.example.com/qualifications/iso9001.jpg",
      "type": "CERTIFICATE",
      "status": 1,
      "createTime": "2023-12-01T10:00:00"
    }
  ],
  "timestamp": "2023-12-01T10:00:00"
}
```

### 4.3 根据类型获取资质文件
**请求方式**: `GET`
**接口地址**: `/api/public/company/qualifications/type/{type}`
**是否需要认证**: 否

**路径参数:**
- `type` (String): 资质类型 (BUSINESS_LICENSE, CERTIFICATE, HONOR)

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/public/company/qualifications/type/BUSINESS_LICENSE
```

### 4.4 获取核心团队成员
**请求方式**: `GET`
**接口地址**: `/api/public/company/team/core`
**是否需要认证**: 否

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/public/company/team/core
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "name": "张三",
      "position": "CEO",
      "department": "管理层",
      "avatar": "https://oss.example.com/team/avatar1.jpg",
      "bio": "拥有10年行业经验",
      "isCore": true,
      "status": 1
    }
  ],
  "timestamp": "2023-12-01T10:00:00"
}
```

### 4.5 获取所有团队成员
**请求方式**: `GET`
**接口地址**: `/api/public/company/team`
**是否需要认证**: 否

### 4.6 根据部门获取团队成员
**请求方式**: `GET`
**接口地址**: `/api/public/company/team/department/{department}`
**是否需要认证**: 否

**路径参数:**
- `department` (String): 部门名称

### 4.7 获取营业执照
**请求方式**: `GET`
**接口地址**: `/api/public/company/qualification/business-license`
**是否需要认证**: 否

### 4.8 获取认证证书
**请求方式**: `GET`
**接口地址**: `/api/public/company/qualification/certificates`
**是否需要认证**: 否

### 4.9 获取荣誉证书
**请求方式**: `GET`
**接口地址**: `/api/public/company/qualification/honors`
**是否需要认证**: 否

---

## 5. 产品管理接口

### 5.1 创建新产品（包含完整产品属性）
**请求方式**: `POST`
**接口地址**: `/api/products`
**是否需要认证**: 是

**功能描述**: 创建新产品，支持设置所有产品属性，包括服装类专业属性

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| **基本信息** | | | |
| productName | String | 是 | 产品名称，最大200字符 |
| productCode | String | 否 | 产品编码，最大100字符 |
| category | String | 否 | 产品分类，最大100字符 |
| description | String | 否 | 产品描述，最大2000字符 |
| specifications | String | 否 | 产品规格，最大500字符 |
| **媒体资源** | | | |
| mainImage | String | 否 | 产品主图URL，最大500字符 |
| productImages | String | 否 | 产品图片列表JSON格式 |
| videoPath | String | 否 | 产品视频路径，最大500字符 |
| **价格库存** | | | |
| price | BigDecimal | 否 | 产品价格，不能为负数 |
| marketPrice | BigDecimal | 否 | 市场价格，不能为负数 |
| stockQuantity | Integer | 否 | 库存数量，不能为负数，默认0 |
| **服装材质属性** | | | |
| material | String | 否 | 材质，最大100字符（如：纯棉、涤纶、牛仔布） |
| fabricType | String | 否 | 面料类型，最大100字符（如：梭织布、针织布） |
| fabricWeight | String | 否 | 面料克重，最大50字符（如：180g、220g） |
| **服装款式属性** | | | |
| waistStyle | String | 否 | 腰型，最大50字符（如：高腰、中腰、低腰） |
| jeansStyle | String | 否 | 牛仔裤款式，最大50字符（如：直筒、紧身、喇叭） |
| patternFit | String | 否 | 板型，最大50字符（如：修身、宽松、标准） |
| style | String | 否 | 样式，最大100字符（如：休闲、商务、运动） |
| **工艺制造属性** | | | |
| craftsmanship | String | 否 | 工艺，最大200字符（如：水洗、石磨、激光） |
| weavingMethod | String | 否 | 编织方法，最大100字符（如：平纹、斜纹、提花） |
| washingProcess | String | 否 | 洗水工艺，最大100字符（如：普洗、石洗、酵洗） |
| needleDetectionProcess | String | 否 | 检针工艺，最大50字符（如：过检、磁检） |
| **图案设计属性** | | | |
| patternType | String | 否 | 图案类型，最大100字符（如：纯色、条纹、印花） |
| printingMethod | String | 否 | 印花方法，最大100字符（如：丝印、数码印花） |
| fashionElements | String | 否 | 流行元素，最大200字符（如：破洞、刺绣、撞色） |
| logoPosition | String | 否 | 徽标位置，最大100字符（如：左胸、后领、袖口） |
| **供应商业属性** | | | |
| supplyType | String | 否 | 供应类型，最大50字符（如：现货、订做、批发） |
| origin | String | 否 | 原产地，最大100字符（如：中国、越南、孟加拉） |
| fastSampling | Boolean | 否 | 7天快速打样：false-否 true-是 |
| **规格包装属性** | | | |
| model | String | 否 | 型号，最大50字符（如：S/M/L、均码） |
| season | String | 否 | 季节，最大50字符（如：春季、夏季、四季） |
| salesUnit | String | 否 | 销售单位，最大50字符（如：件、套、打） |
| **其他属性** | | | |
| tags | List<String> | 否 | 产品标签列表 |
| status | Integer | 否 | 产品状态：0-下架 1-上架 2-草稿，默认1 |
| isFeatured | Boolean | 否 | 是否推荐：false-否 true-是，默认false |
| sortOrder | Integer | 否 | 排序值，不能为负数，默认0 |
| seoKeywords | String | 否 | SEO关键词，最大200字符 |
| seoDescription | String | 否 | SEO描述，最大500字符 |

**请求示例:**
```bash
curl -X POST http://localhost:33380/api/products \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "productName": "商务休闲牛仔裤",
    "productCode": "JEANS-001",
    "category": "服装",
    "description": "高品质商务休闲牛仔裤，采用优质牛仔布面料，舒适透气，款式时尚",
    "specifications": "修身版型，经典蓝色，适合商务休闲场合",
    "mainImage": "https://oss.example.com/products/jeans-main.jpg",
    "productImages": "[\"https://oss.example.com/products/jeans-1.jpg\", \"https://oss.example.com/products/jeans-2.jpg\"]",
    "videoPath": "/uploads/videos/product/jeans-demo.mp4",
    "price": 299.00,
    "marketPrice": 399.00,
    "stockQuantity": 500,
    "material": "98%棉 2%氨纶",
    "fabricType": "牛仔布",
    "fabricWeight": "320g",
    "waistStyle": "中腰",
    "jeansStyle": "直筒",
    "patternFit": "修身",
    "style": "商务休闲",
    "craftsmanship": "水洗做旧工艺",
    "weavingMethod": "斜纹编织",
    "washingProcess": "石洗酵洗",
    "needleDetectionProcess": "磁检",
    "patternType": "纯色",
    "printingMethod": "无",
    "fashionElements": "简约经典，金属扣装饰",
    "logoPosition": "后腰右侧",
    "supplyType": "现货供应",
    "origin": "中国",
    "fastSampling": true,
    "model": "S/M/L/XL/XXL",
    "season": "四季",
    "salesUnit": "件",
    "tags": ["牛仔裤", "商务", "休闲", "修身"],
    "status": 1,
    "isFeatured": true,
    "sortOrder": 10,
    "seoKeywords": "牛仔裤,商务休闲,修身,高品质",
    "seoDescription": "高品质商务休闲牛仔裤，采用优质牛仔布面料，舒适透气，修身版型"
  }'
```

**响应示例:**
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1,
    "productName": "商务休闲牛仔裤",
    "productCode": "JEANS-001",
    "category": "服装",
    "description": "高品质商务休闲牛仔裤，采用优质牛仔布面料，舒适透气，款式时尚",
    "specifications": "修身版型，经典蓝色，适合商务休闲场合",
    "mainImage": "https://oss.example.com/products/jeans-main.jpg",
    "productImages": "[\"https://oss.example.com/products/jeans-1.jpg\", \"https://oss.example.com/products/jeans-2.jpg\"]",
    "videoPath": "/uploads/videos/product/jeans-demo.mp4",
    "price": 299.00,
    "marketPrice": 399.00,
    "stockQuantity": 500,
    "material": "98%棉 2%氨纶",
    "fabricType": "牛仔布",
    "fabricWeight": "320g",
    "waistStyle": "中腰",
    "jeansStyle": "直筒",
    "patternFit": "修身",
    "style": "商务休闲",
    "craftsmanship": "水洗做旧工艺",
    "weavingMethod": "斜纹编织",
    "washingProcess": "石洗酵洗",
    "needleDetectionProcess": "磁检",
    "patternType": "纯色",
    "printingMethod": "无",
    "fashionElements": "简约经典，金属扣装饰",
    "logoPosition": "后腰右侧",
    "supplyType": "现货供应",
    "origin": "中国",
    "fastSampling": true,
    "model": "S/M/L/XL/XXL",
    "season": "四季",
    "salesUnit": "件",
    "tags": ["牛仔裤", "商务", "休闲", "修身"],
    "status": 1,
    "isFeatured": true,
    "sortOrder": 10,
    "seoKeywords": "牛仔裤,商务休闲,修身,高品质",
    "seoDescription": "高品质商务休闲牛仔裤，采用优质牛仔布面料，舒适透气，修身版型",
    "createTime": "2025-11-14T10:00:00",
    "updateTime": "2025-11-14T10:00:00"
  },
  "timestamp": "2025-11-14T10:00:00"
}
```

### 5.2 根据ID获取产品详情
**请求方式**: `GET`
**接口地址**: `/api/products/{id}`
**是否需要认证**: 是

**路径参数:**
- `id` (Long): 产品ID

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/products/1 \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "productName": "智能管理系统",
    "productCode": "SM-001",
    "category": "软件产品",
    "description": "智能化的企业管理系统，具有强大的功能和易用性",
    "specifications": "支持多租户，高可用架构，微服务设计",
    "mainImage": "https://oss.example.com/products/main-image.jpg",
    "productImages": "[\"https://oss.example.com/products/img1.jpg\", \"https://oss.example.com/products/img2.jpg\"]",
    "videoPath": "/uploads/videos/product/demo.mp4",
    "price": 50000.00,
    "marketPrice": 60000.00,
    "stockQuantity": 100,
    "tags": ["企业级", "智能化", "云端", "管理系统"],
    "status": 1,
    "isFeatured": false,
    "sortOrder": 0,
    "seoKeywords": "企业管理系统,智能化,云端",
    "seoDescription": "专业的企业管理系统解决方案，智能化云端管理",
    "createTime": "2025-11-14T10:00:00",
    "updateTime": "2025-11-14T10:00:00"
  },
  "timestamp": "2025-11-14T10:00:00"
}
```

### 5.3 根据编码获取产品详情
**请求方式**: `GET`
**接口地址**: `/api/products/code/{productCode}`
**是否需要认证**: 是

**路径参数:**
- `productCode` (String): 产品编码

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/products/code/SM-001 \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "productName": "智能管理系统",
    "productCode": "SM-001",
    "category": "软件产品",
    "description": "智能化的企业管理系统，具有强大的功能和易用性",
    "specifications": "支持多租户，高可用架构，微服务设计",
    "mainImage": "https://oss.example.com/products/main-image.jpg",
    "productImages": "[\"https://oss.example.com/products/img1.jpg\", \"https://oss.example.com/products/img2.jpg\"]",
    "videoPath": "/uploads/videos/product/demo.mp4",
    "price": 50000.00,
    "marketPrice": 60000.00,
    "stockQuantity": 100,
    "tags": ["企业级", "智能化", "云端", "管理系统"],
    "status": 1,
    "isFeatured": false,
    "sortOrder": 0,
    "seoKeywords": "企业管理系统,智能化,云端",
    "seoDescription": "专业的企业管理系统解决方案，智能化云端管理",
    "createTime": "2025-11-14T10:00:00",
    "updateTime": "2025-11-14T10:00:00"
  },
  "timestamp": "2025-11-14T10:00:00"
}
```

### 5.4 分页查询产品列表
**请求方式**: `GET`
**接口地址**: `/api/products`
**是否需要认证**: 是

**查询参数:**
- `page` (Integer): 页码 (默认0)
- `size` (Integer): 每页大小 (默认10)
- `category` (String): 产品分类 (可选)
- `status` (Integer): 产品状态 (可选)
- `keyword` (String): 搜索关键词 (可选)
- `minPrice` (Double): 最低价格 (可选)
- `maxPrice` (Double): 最高价格 (可选)

**请求示例:**
```bash
curl -X GET "http://localhost:33380/api/products?page=0&size=10&category=软件产品&status=1" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "content": [
      {
        "id": 1,
        "productName": "智能管理系统",
        "productCode": "SM-001",
        "category": "软件产品",
        "description": "智能化的企业管理系统，具有强大的功能和易用性",
        "specifications": "支持多租户，高可用架构，微服务设计",
        "mainImage": "https://oss.example.com/products/main-image.jpg",
        "productImages": "[\"https://oss.example.com/products/img1.jpg\", \"https://oss.example.com/products/img2.jpg\"]",
        "videoPath": "/uploads/videos/product/demo.mp4",
        "price": 50000.00,
        "marketPrice": 60000.00,
        "stockQuantity": 100,
        "material": "98%棉 2%氨纶",
        "fabricType": "牛仔布",
        "fabricWeight": "320g",
        "waistStyle": "中腰",
        "jeansStyle": "直筒",
        "patternFit": "修身",
        "style": "商务休闲",
        "craftsmanship": "水洗做旧工艺",
        "weavingMethod": "斜纹编织",
        "washingProcess": "石洗酵洗",
        "needleDetectionProcess": "磁检",
        "patternType": "纯色",
        "printingMethod": "无",
        "fashionElements": "简约经典，金属扣装饰",
        "logoPosition": "后腰右侧",
        "supplyType": "现货供应",
        "origin": "中国",
        "fastSampling": true,
        "model": "S/M/L/XL/XXL",
        "season": "四季",
        "salesUnit": "件",
        "tags": ["企业级", "智能化", "云端", "管理系统"],
        "status": 1,
        "isFeatured": false,
        "sortOrder": 0,
        "seoKeywords": "企业管理系统,智能化,云端",
        "seoDescription": "专业的企业管理系统解决方案，具有强大的功能和易用性",
        "createTime": "2025-11-14T10:00:00",
        "updateTime": "2025-11-14T10:00:00"
      }
    ],
    "totalElements": 1,
    "totalPages": 1,
    "size": 10,
    "number": 0,
    "first": true,
    "last": true
  },
  "timestamp": "2025-11-14T10:00:00"
}
```

### 5.5 更新产品信息（完整属性编辑）
**请求方式**: `PUT`
**接口地址**: `/api/products/{id}`
**是否需要认证**: 是

**功能描述**: 完整更新产品所有属性信息，这是主要的产品属性编辑接口

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| **基本信息** | | | |
| productName | String | 是 | 产品名称，最大200字符 |
| productCode | String | 否 | 产品编码，最大100字符 |
| category | String | 否 | 产品分类，最大100字符 |
| description | String | 否 | 产品描述，最大2000字符 |
| specifications | String | 否 | 产品规格，最大500字符 |
| **媒体资源** | | | |
| mainImage | String | 否 | 产品主图URL，最大500字符 |
| productImages | String | 否 | 产品图片列表JSON格式 |
| videoPath | String | 否 | 产品视频路径，最大500字符 |
| **价格库存** | | | |
| price | BigDecimal | 否 | 产品价格，不能为负数 |
| marketPrice | BigDecimal | 否 | 市场价格，不能为负数 |
| stockQuantity | Integer | 否 | 库存数量，不能为负数，默认0 |
| **服装材质属性** | | | |
| material | String | 否 | 材质，最大100字符（如：纯棉、涤纶、牛仔布） |
| fabricType | String | 否 | 面料类型，最大100字符（如：梭织布、针织布） |
| fabricWeight | String | 否 | 面料克重，最大50字符（如：180g、220g） |
| **服装款式属性** | | | |
| waistStyle | String | 否 | 腰型，最大50字符（如：高腰、中腰、低腰） |
| jeansStyle | String | 否 | 牛仔裤款式，最大50字符（如：直筒、紧身、喇叭） |
| patternFit | String | 否 | 板型，最大50字符（如：修身、宽松、标准） |
| style | String | 否 | 样式，最大100字符（如：休闲、商务、运动） |
| **工艺制造属性** | | | |
| craftsmanship | String | 否 | 工艺，最大200字符（如：水洗、石磨、激光） |
| weavingMethod | String | 否 | 编织方法，最大100字符（如：平纹、斜纹、提花） |
| washingProcess | String | 否 | 洗水工艺，最大100字符（如：普洗、石洗、酵洗） |
| needleDetectionProcess | String | 否 | 检针工艺，最大50字符（如：过检、磁检） |
| **图案设计属性** | | | |
| patternType | String | 否 | 图案类型，最大100字符（如：纯色、条纹、印花） |
| printingMethod | String | 否 | 印花方法，最大100字符（如：丝印、数码印花） |
| fashionElements | String | 否 | 流行元素，最大200字符（如：破洞、刺绣、撞色） |
| logoPosition | String | 否 | 徽标位置，最大100字符（如：左胸、后领、袖口） |
| **供应商业属性** | | | |
| supplyType | String | 否 | 供应类型，最大50字符（如：现货、订做、批发） |
| origin | String | 否 | 原产地，最大100字符（如：中国、越南、孟加拉） |
| fastSampling | Boolean | 否 | 7天快速打样：false-否 true-是 |
| **规格包装属性** | | | |
| model | String | 否 | 型号，最大50字符（如：S/M/L、均码） |
| season | String | 否 | 季节，最大50字符（如：春季、夏季、四季） |
| salesUnit | String | 否 | 销售单位，最大50字符（如：件、套、打） |
| **其他属性** | | | |
| tags | List<String> | 否 | 产品标签列表 |
| status | Integer | 否 | 产品状态：0-下架 1-上架 2-草稿，默认1 |
| isFeatured | Boolean | 否 | 是否推荐：false-否 true-是，默认false |
| sortOrder | Integer | 否 | 排序值，不能为负数，默认0 |
| seoKeywords | String | 否 | SEO关键词，最大200字符 |
| seoDescription | String | 否 | SEO描述，最大500字符 |

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/products/1 \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "productName": "智能管理系统v2.0",
    "productCode": "SM-001-V2",
    "category": "软件产品",
    "description": "升级版的智能化企业管理系统，具有更强的性能和功能",
    "specifications": "支持多租户，高可用架构，微服务设计",
    "mainImage": "https://oss.example.com/products/main-image.jpg",
    "productImages": "[\"https://oss.example.com/products/img1.jpg\", \"https://oss.example.com/products/img2.jpg\"]",
    "videoPath": "/uploads/videos/product/demo.mp4",
    "price": 60000.00,
    "marketPrice": 75000.00,
    "stockQuantity": 50,
    "material": "98%棉 2%氨纶",
    "fabricType": "牛仔布",
    "fabricWeight": "320g",
    "waistStyle": "中腰",
    "jeansStyle": "直筒",
    "patternFit": "修身",
    "style": "商务休闲",
    "craftsmanship": "水洗做旧工艺",
    "weavingMethod": "斜纹编织",
    "washingProcess": "石洗酵洗",
    "needleDetectionProcess": "磁检",
    "patternType": "纯色",
    "printingMethod": "无",
    "fashionElements": "简约经典，金属扣装饰",
    "logoPosition": "后腰右侧",
    "supplyType": "现货供应",
    "origin": "中国",
    "fastSampling": true,
    "model": "S/M/L/XL/XXL",
    "season": "四季",
    "salesUnit": "件",
    "tags": ["企业级", "智能化", "云端", "升级版"],
    "status": 1,
    "isFeatured": true,
    "sortOrder": 10,
    "seoKeywords": "企业管理系统,智能化,升级版",
    "seoDescription": "专业的企业管理系统解决方案，升级版具有更强的性能和功能"
  }'
```

**响应示例**:
```json
{
  "code": 200,
  "message": "产品更新成功",
  "data": {
    "id": 1,
    "productName": "智能管理系统v2.0",
    "productCode": "SM-001-V2",
    "category": "软件产品",
    "description": "升级版的智能化企业管理系统，具有更强的性能和功能",
    "specifications": "支持多租户，高可用架构，微服务设计",
    "mainImage": "https://oss.example.com/products/main-image.jpg",
    "productImages": "[\"https://oss.example.com/products/img1.jpg\", \"https://oss.example.com/products/img2.jpg\"]",
    "videoPath": "/uploads/videos/product/demo.mp4",
    "price": 60000.00,
    "marketPrice": 75000.00,
    "stockQuantity": 50,
    "material": "98%棉 2%氨纶",
    "fabricType": "牛仔布",
    "fabricWeight": "320g",
    "waistStyle": "中腰",
    "jeansStyle": "直筒",
    "patternFit": "修身",
    "style": "商务休闲",
    "craftsmanship": "水洗做旧工艺",
    "weavingMethod": "斜纹编织",
    "washingProcess": "石洗酵洗",
    "needleDetectionProcess": "磁检",
    "patternType": "纯色",
    "printingMethod": "无",
    "fashionElements": "简约经典，金属扣装饰",
    "logoPosition": "后腰右侧",
    "supplyType": "现货供应",
    "origin": "中国",
    "fastSampling": true,
    "model": "S/M/L/XL/XXL",
    "season": "四季",
    "salesUnit": "件",
    "tags": ["企业级", "智能化", "云端", "升级版"],
    "status": 1,
    "isFeatured": true,
    "sortOrder": 10,
    "seoKeywords": "企业管理系统,智能化,升级版",
    "seoDescription": "专业的企业管理系统解决方案，升级版具有更强的性能和功能",
    "updateTime": "2025-11-14T10:00:00"
  },
  "timestamp": "2025-11-14T10:00:00"
}
```

### 5.6 删除产品
**请求方式**: `DELETE`
**接口地址**: `/api/products/{id}`
**是否需要认证**: 是

**请求示例:**
```bash
curl -X DELETE http://localhost:33380/api/products/1 \
  -H "Authorization: Bearer your-jwt-token"
```

### 5.7 保存产品为草稿
**请求方式**: `PUT`
**接口地址**: `/api/products/{id}/save-draft`
**是否需要认证**: 是

**功能描述**: 将产品保存为草稿状态

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/products/1/save-draft \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "产品保存为草稿成功",
  "data": {
    "id": 1,
    "productName": "智能管理系统",
    "status": 2,
    "statusText": "草稿",
    "updateTime": "2025-11-14T10:00:00"
  },
  "timestamp": "2025-11-14T10:00:00"
}
```

### 5.8 从草稿发布产品
**请求方式**: `PUT`
**接口地址**: `/api/products/{id}/publish`
**是否需要认证**: 是

**功能描述**: 将草稿状态的产品发布为上架状态

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/products/1/publish \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "产品从草稿发布成功",
  "data": {
    "id": 1,
    "productName": "智能管理系统",
    "status": 1,
    "statusText": "已上架",
    "updateTime": "2025-11-14T10:00:00"
  },
  "timestamp": "2025-11-14T10:00:00"
}
```

### 5.9 获取草稿产品列表
**请求方式**: `GET`
**接口地址**: `/api/products/status/drafts`
**是否需要认证**: 是

**功能描述**: 获取所有草稿状态的产品列表

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/products/status/drafts \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "productName": "草稿产品1",
      "status": 2,
      "statusText": "草稿",
      "createTime": "2025-11-14T09:00:00"
    }
  ],
  "timestamp": "2025-11-14T10:00:00"
}
```

### 5.10 上架产品
**请求方式**: `PUT`
**接口地址**: `/api/products/{id}/activate`
**是否需要认证**: 是

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/products/1/activate \
  -H "Authorization: Bearer your-jwt-token"
```

### 5.8 下架产品
**请求方式**: `PUT`
**接口地址**: `/api/products/{id}/deactivate`
**是否需要认证**: 是

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/products/1/deactivate \
  -H "Authorization: Bearer your-jwt-token"
```

### 5.9 更新产品库存
**请求方式**: `PUT`
**接口地址**: `/api/products/{id}/stock`
**是否需要认证**: 是

**查询参数:**
- `quantity` (Integer): 新的库存数量

**请求示例:**
```bash
curl -X PUT "http://localhost:33380/api/products/1/stock?quantity=150" \
  -H "Authorization: Bearer your-jwt-token"
```

### 5.10 设置产品为推荐
**请求方式**: `PUT`
**接口地址**: `/api/products/{id}/featured`
**是否需要认证**: 是

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/products/1/featured \
  -H "Authorization: Bearer your-jwt-token"
```

### 5.11 取消产品推荐
**请求方式**: `DELETE`
**接口地址**: `/api/products/{id}/featured`
**是否需要认证**: 是

**请求示例:**
```bash
curl -X DELETE http://localhost:33380/api/products/1/featured \
  -H "Authorization: Bearer your-jwt-token"
```

### 5.12 搜索产品
**请求方式**: `GET`
**接口地址**: `/api/products/search`
**是否需要认证**: 是

**查询参数:**
- `keyword` (String): 搜索关键词

**请求示例:**
```bash
curl -X GET "http://localhost:33380/api/products/search?keyword=智能" \
  -H "Authorization: Bearer your-jwt-token"
```

### 5.13 根据分类获取产品列表
**请求方式**: `GET`
**接口地址**: `/api/products/category/{category}`
**是否需要认证**: 是

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/products/category/软件产品 \
  -H "Authorization: Bearer your-jwt-token"
```

### 5.14 根据标签查找产品
**请求方式**: `GET`
**接口地址**: `/api/products/tag/{tag}`
**是否需要认证**: 是

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/products/tag/企业级 \
  -H "Authorization: Bearer your-jwt-token"
```

### 5.15 根据价格区间查找产品
**请求方式**: `GET`
**接口地址**: `/api/products/price-range`
**是否需要认证**: 是

**查询参数:**
- `minPrice` (Double): 最低价格
- `maxPrice` (Double): 最高价格

**请求示例:**
```bash
curl -X GET "http://localhost:33380/api/products/price-range?minPrice=10000&maxPrice=100000" \
  -H "Authorization: Bearer your-jwt-token"
```

### 5.16 获取上架产品列表
**请求方式**: `GET`
**接口地址**: `/api/products/active`
**是否需要认证**: 是

**功能描述**: 获取所有上架状态的产品列表

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/products/active \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "productName": "智能管理系统",
      "productCode": "SM-001",
      "category": "软件产品",
      "description": "智能化的企业管理系统，具有强大的功能和易用性",
      "mainImage": "https://oss.example.com/products/main-image.jpg",
      "videoPath": "/uploads/videos/product/demo.mp4",
      "price": 50000.00,
      "marketPrice": 60000.00,
      "stockQuantity": 100,
      "tags": ["企业级", "智能化", "云端"],
      "status": 1,
      "isFeatured": false,
      "sortOrder": 0,
      "createTime": "2025-11-14T10:00:00",
      "updateTime": "2025-11-14T10:00:00"
    }
  ],
  "timestamp": "2025-11-14T10:00:00"
}
```

### 5.17 获取推荐产品列表
**请求方式**: `GET`
**接口地址**: `/api/products/featured`
**是否需要认证**: 是

**功能描述**: 获取所有推荐状态的产品列表

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/products/featured \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "productName": "智能管理系统",
      "productCode": "SM-001",
      "category": "软件产品",
      "description": "智能化的企业管理系统，具有强大的功能和易用性",
      "mainImage": "https://oss.example.com/products/main-image.jpg",
      "videoPath": "/uploads/videos/product/demo.mp4",
      "price": 50000.00,
      "marketPrice": 60000.00,
      "stockQuantity": 100,
      "tags": ["企业级", "智能化", "云端"],
      "status": 1,
      "isFeatured": true,
      "sortOrder": 10,
      "createTime": "2025-11-14T10:00:00",
      "updateTime": "2025-11-14T10:00:00"
    }
  ],
  "timestamp": "2025-11-14T10:00:00"
}
```

### 5.18 获取产品统计信息
**请求方式**: `GET`
**接口地址**: `/api/products/statistics`
**是否需要认证**: 是

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/products/statistics \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "totalProducts": 50,
    "activeProducts": 45,
    "featuredProducts": 10,
    "categories": [
      {"name": "软件产品", "count": 30},
      {"name": "硬件产品", "count": 20}
    ],
    "totalValue": 2500000.00
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 5.19 高级搜索产品

**请求方式**: `GET`
**接口地址**: `/api/products/search/advanced`
**是否需要认证**: 否

**查询参数**:
- `keyword` (String): 搜索关键词 (必填)
- `category` (String): 产品分类 (可选)

**请求示例**:
```bash
curl -X GET "http://localhost:33380/api/products/search/advanced?keyword=智能&category=软件产品"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "搜索成功",
  "data": [
    {
      "id": 1,
      "productName": "智能管理系统",
      "productCode": "SM-001",
      "category": "软件产品",
      "price": 50000.00,
      "stock": 100,
      "status": 1
    }
  ],
  "timestamp": "2023-12-01T10:00:00"
}
```

### 5.20 产品图片管理接口

#### 5.20.1 上传产品图片
**请求方式**: `POST`
**接口地址**: `/api/products/images`
**是否需要认证**: 是

**请求参数**:
```json
{
  "productId": 1,
  "imageUrls": [
    "https://oss.example.com/product1.jpg",
    "https://oss.example.com/product2.jpg"
  ]
}
```

**请求示例:**
```bash
curl -X POST http://localhost:33380/api/products/images \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "productId": 1,
    "imageUrls": [
      "https://oss.example.com/product1.jpg",
      "https://oss.example.com/product2.jpg"
    ]
  }'
```

#### 5.20.2 删除产品图片
**请求方式**: `DELETE`
**接口地址**: `/api/products/{productId}/images`
**是否需要认证**: 是

**查询参数**:
- `imageUrl` (String): 要删除的图片URL

**请求示例:**
```bash
curl -X DELETE "http://localhost:33380/api/products/1/images?imageUrl=https://oss.example.com/product1.jpg" \
  -H "Authorization: Bearer your-jwt-token"
```

#### 5.20.3 设置产品主图
**请求方式**: `PUT`
**接口地址**: `/api/products/{productId}/main-image`
**是否需要认证**: 是

**查询参数**:
- `imageUrl` (String): 主图URL

**请求示例:**
```bash
curl -X PUT "http://localhost:33380/api/products/1/main-image?imageUrl=https://oss.example.com/product-main.jpg" \
  -H "Authorization: Bearer your-jwt-token"
```

### 5.21 检查产品编码是否存在
**请求方式**: `GET`
**接口地址**: `/api/products/check-code/{productCode}`
**是否需要认证**: 是

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/products/check-code/SM-001 \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "检查完成",
  "data": true,
  "timestamp": "2023-12-01T10:00:00"
}
```

### 5.22 产品视频管理接口

#### 5.22.1 上传产品视频文件
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/product/video`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**功能描述**: 上传产品视频文件到服务器

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | MultipartFile | 是 | 视频文件 |

**支持的视频格式**:
- MP4 (推荐)
- AVI
- MOV
- WMV
- FLV
- WebM
- MKV
- M4V

**文件大小限制**: 150MB

**请求示例**:
```bash
curl -X POST http://localhost:33380/api/admin/upload/product/video \
  -H "Authorization: Bearer your-jwt-token" \
  -F "file=@/path/to/product-video.mp4"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "产品视频上传成功",
  "data": {
    "url": "/uploads/videos/product/2025/11/13/example-video.mp4",
    "fileName": "example-video.mp4",
    "fileSize": "52428800"
  },
  "success": true,
  "timestamp": 1731513480000
}
```

#### 5.22.2 关联视频到产品
**请求方式**: `POST`
**接口地址**: `/api/products/video`
**是否需要认证**: 是

**功能描述**: 将已上传的视频文件关联到指定产品

**请求参数**:
```json
{
  "productId": 20,
  "videoUrl": "/uploads/videos/product/2025/11/13/example-video.mp4",
  "videoTitle": "产品展示视频",
  "videoDescription": "这是产品的详细介绍和演示视频"
}
```

**请求示例**:
```bash
curl -X POST http://localhost:33380/api/products/video \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "productId": 20,
    "videoUrl": "/uploads/videos/product/2025/11/13/example-video.mp4",
    "videoTitle": "产品展示视频",
    "videoDescription": "这是产品的详细介绍和演示视频"
  }'
```

**响应示例**:
```json
{
  "code": 200,
  "message": "视频关联成功",
  "data": {
    "id": 20,
    "productName": "测试产品",
    "videoPath": "/uploads/videos/product/2025/11/13/example-video.mp4",
    "updateTime": "2025-11-13T14:30:00"
  },
  "success": true,
  "timestamp": 1731513480000
}
```

#### 5.22.3 删除产品视频
**请求方式**: `DELETE`
**接口地址**: `/api/products/{productId}/video`
**是否需要认证**: 是

**功能描述**: 删除指定产品的视频关联（仅删除数据库中的关联记录，不删除实际文件）

**路径参数**:
- `productId` (Long): 产品ID

**请求示例**:
```bash
curl -X DELETE http://localhost:33380/api/products/20/video \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "产品视频删除成功",
  "data": {
    "id": 20,
    "productName": "测试产品",
    "videoPath": null,
    "updateTime": "2025-11-13T15:00:00"
  },
  "success": true,
  "timestamp": 1731513480000
}
```

#### 5.22.4 批量操作产品
##### 5.22.4.1 批量删除产品
**请求方式**: `DELETE`
**接口地址**: `/api/products/batch`
**是否需要认证**: 是

**功能描述**: 批量删除多个产品

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X DELETE http://localhost:33380/api/products/batch \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

##### 5.22.4.2 批量上架产品
**请求方式**: `PUT`
**接口地址**: `/api/products/batch/activate`
**是否需要认证**: 是

**功能描述**: 批量上架多个产品

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/products/batch/activate \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

##### 5.22.4.3 批量下架产品
**请求方式**: `PUT`
**接口地址**: `/api/products/batch/deactivate`
**是否需要认证**: 是

**功能描述**: 批量下架多个产品

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/products/batch/deactivate \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

##### 5.22.4.4 批量保存为草稿
**请求方式**: `PUT`
**接口地址**: `/api/products/batch/save-draft`
**是否需要认证**: 是

**功能描述**: 批量将多个产品保存为草稿状态

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/products/batch/save-draft \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

##### 5.22.4.5 批量从草稿发布
**请求方式**: `PUT`
**接口地址**: `/api/products/batch/publish`
**是否需要认证**: 是

**功能描述**: 批量将草稿状态的产品发布

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/products/batch/publish \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

---

## 6. 新品管理接口

### 6.1 分页查询新品列表
**请求方式**: `POST`
**接口地址**: `/api/new-products`
**是否需要认证**: 是

**功能描述**: 分页获取新品列表

**请求参数**:
```json
{
  "page": 0,
  "size": 10,
  "category": "服装",
  "status": 1,
  "keyword": "搜索关键词"
}
```

**请求示例:**
```bash
curl -X POST http://localhost:33380/api/new-products \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "page": 0,
    "size": 10,
    "category": "服装",
    "status": 1
  }'
```

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "content": [
      {
        "id": 1,
        "productName": "新品牛仔裤",
        "productCode": "NEW-001",
        "category": "服装",
        "price": 299.00,
        "isNew": true,
        "status": 1,
        "createTime": "2025-11-21T10:00:00"
      }
    ],
    "totalElements": 1,
    "totalPages": 1,
    "size": 10,
    "number": 0
  },
  "timestamp": "2025-11-21T10:00:00"
}
```

### 6.2 获取有效新品列表
**请求方式**: `GET`
**接口地址**: `/api/new-products/active`
**是否需要认证**: 是

**功能描述**: 获取所有有效状态的新品列表

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/new-products/active \
  -H "Authorization: Bearer your-jwt-token"
```

### 6.3 设置产品为新品
**请求方式**: `PUT`
**接口地址**: `/api/new-products/{id}`
**是否需要认证**: 是

**功能描述**: 将指定产品设置为新品

**路径参数:**
- `id` (Long): 产品ID

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/new-products/1 \
  -H "Authorization: Bearer your-jwt-token"
```

### 6.4 取消产品新品标识
**请求方式**: `DELETE`
**接口地址**: `/api/new-products/{id}`
**是否需要认证**: 是

**功能描述**: 取消指定产品的新品标识

**路径参数:**
- `id` (Long): 产品ID

**请求示例:**
```bash
curl -X DELETE http://localhost:33380/api/new-products/1 \
  -H "Authorization: Bearer your-jwt-token"
```

### 6.5 批量设置产品为新品
**请求方式**: `PUT`
**接口地址**: `/api/new-products/batch`
**是否需要认证**: 是

**功能描述**: 批量将多个产品设置为新品

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/new-products/batch \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

### 6.6 批量取消产品新品标识
**请求方式**: `DELETE`
**接口地址**: `/api/new-products/batch`
**是否需要认证**: 是

**功能描述**: 批量取消多个产品的新品标识

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X DELETE http://localhost:33380/api/new-products/batch \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

### 6.7 获取新品统计信息
**请求方式**: `GET`
**接口地址**: `/api/new-products/statistics`
**是否需要认证**: 是

**功能描述**: 获取新品的统计信息

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/new-products/statistics \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "totalNewProducts": 15,
    "activeNewProducts": 12,
    "inactiveNewProducts": 3,
    "categories": [
      {"name": "服装", "count": 8},
      {"name": "配饰", "count": 4}
    ]
  },
  "timestamp": "2025-11-21T10:00:00"
}
```

---

## 7. 主要产品管理接口

### 7.1 分页查询主要产品列表
**请求方式**: `POST`
**接口地址**: `/api/main-products`
**是否需要认证**: 是

**功能描述**: 分页获取主要产品列表

**请求参数**:
```json
{
  "page": 0,
  "size": 10,
  "category": "服装",
  "status": 1,
  "keyword": "搜索关键词"
}
```

**请求示例:**
```bash
curl -X POST http://localhost:33380/api/main-products \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "page": 0,
    "size": 10,
    "category": "服装",
    "status": 1
  }'
```

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "content": [
      {
        "id": 1,
        "productName": "主打产品牛仔裤",
        "productCode": "MAIN-001",
        "category": "服装",
        "price": 399.00,
        "isMain": true,
        "status": 1,
        "createTime": "2025-11-21T10:00:00"
      }
    ],
    "totalElements": 1,
    "totalPages": 1,
    "size": 10,
    "number": 0
  },
  "timestamp": "2025-11-21T10:00:00"
}
```

### 7.2 获取有效主要产品列表
**请求方式**: `GET`
**接口地址**: `/api/main-products/active`
**是否需要认证**: 是

**功能描述**: 获取所有有效状态的主要产品列表

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/main-products/active \
  -H "Authorization: Bearer your-jwt-token"
```

### 7.3 设置产品为主要产品
**请求方式**: `PUT`
**接口地址**: `/api/main-products/{id}`
**是否需要认证**: 是

**功能描述**: 将指定产品设置为主要产品

**路径参数:**
- `id` (Long): 产品ID

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/main-products/1 \
  -H "Authorization: Bearer your-jwt-token"
```

### 7.4 取消产品主要产品标识
**请求方式**: `DELETE`
**接口地址**: `/api/main-products/{id}`
**是否需要认证**: 是

**功能描述**: 取消指定产品的主要产品标识

**路径参数:**
- `id` (Long): 产品ID

**请求示例:**
```bash
curl -X DELETE http://localhost:33380/api/main-products/1 \
  -H "Authorization: Bearer your-jwt-token"
```

### 7.5 批量设置产品为主要产品
**请求方式**: `PUT`
**接口地址**: `/api/main-products/batch`
**是否需要认证**: 是

**功能描述**: 批量将多个产品设置为主要产品

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/main-products/batch \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

### 7.6 批量取消产品主要产品标识
**请求方式**: `DELETE`
**接口地址**: `/api/main-products/batch`
**是否需要认证**: 是

**功能描述**: 批量取消多个产品的主要产品标识

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X DELETE http://localhost:33380/api/main-products/batch \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

### 7.7 获取主要产品统计信息
**请求方式**: `GET`
**接口地址**: `/api/main-products/statistics`
**是否需要认证**: 是

**功能描述**: 获取主要产品的统计信息

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/main-products/statistics \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "totalMainProducts": 20,
    "activeMainProducts": 18,
    "inactiveMainProducts": 2,
    "categories": [
      {"name": "服装", "count": 12},
      {"name": "配饰", "count": 6}
    ]
  },
  "timestamp": "2025-11-21T10:00:00"
}
```

---

## 8. 富文本内容管理接口

### 8.1 创建富文本内容
**请求方式**: `POST`
**接口地址**: `/api/rich-content`
**是否需要认证**: 是

**功能描述**: 创建新的富文本内容

**请求参数:**
```json
{
  "title": "公司新闻标题",
  "content": "<p>这是一篇公司新闻的HTML内容</p>",
  "contentType": "NEWS",
  "summary": "新闻摘要",
  "author": "张三",
  "tags": ["新闻", "公告"],
  "relatedId": 1,
  "relatedType": "COMPANY",
  "status": 1
}
```

**请求示例:**
```bash
curl -X POST http://localhost:33380/api/rich-content \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "公司新闻标题",
    "content": "<p>这是一篇公司新闻的HTML内容</p>",
    "contentType": "NEWS",
    "summary": "新闻摘要",
    "author": "张三",
    "tags": ["新闻", "公告"],
    "relatedId": 1,
    "relatedType": "COMPANY",
    "status": 1
  }'
```

### 8.2 根据ID获取内容详情
**请求方式**: `GET`
**接口地址**: `/api/rich-content/{id}`
**是否需要认证**: 是

**功能描述**: 根据ID获取富文本内容详情

**路径参数:**
- `id` (Long): 内容ID

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/rich-content/1 \
  -H "Authorization: Bearer your-jwt-token"
```

### 8.3 获取公开内容详情
**请求方式**: `GET`
**接口地址**: `/api/rich-content/public/{id}`
**是否需要认证**: 否

**功能描述**: 获取公开的富文本内容详情

**路径参数:**
- `id` (Long): 内容ID

**请求示例:**
```bash
curl -X GET http://localhost:33380/api/rich-content/public/1
```

### 8.4 分页查询内容列表
**请求方式**: `GET`
**接口地址**: `/api/rich-content`
**是否需要认证**: 是

**功能描述**: 分页查询富文本内容列表

**查询参数:**
- `page` (Integer): 页码 (默认0)
- `size` (Integer): 每页大小 (默认10)
- `contentType` (String): 内容类型 (可选)
- `status` (Integer): 内容状态 (可选)
- `author` (String): 作者 (可选)
- `keyword` (String): 搜索关键词 (可选)

**请求示例:**
```bash
curl -X GET "http://localhost:33380/api/rich-content?page=0&size=10&contentType=NEWS&status=1" \
  -H "Authorization: Bearer your-jwt-token"
```

### 8.5 更新富文本内容
**请求方式**: `PUT`
**接口地址**: `/api/rich-content/{id}`
**是否需要认证**: 是

**功能描述**: 更新富文本内容

**路径参数:**
- `id` (Long): 内容ID

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/rich-content/1 \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "更新后的标题",
    "content": "<p>更新后的内容</p>"
  }'
```

### 8.6 删除内容
**请求方式**: `DELETE`
**接口地址**: `/api/rich-content/{id}`
**是否需要认证**: 是

**功能描述**: 删除指定的富文本内容

**路径参数:**
- `id` (Long): 内容ID

**请求示例:**
```bash
curl -X DELETE http://localhost:33380/api/rich-content/1 \
  -H "Authorization: Bearer your-jwt-token"
```

### 8.7 发布内容
**请求方式**: `POST`
**接口地址**: `/api/rich-content/publish`
**是否需要认证**: 是

**功能描述**: 发布富文本内容

**请求参数:**
```json
{
  "id": 1,
  "publishTime": "2023-12-01T10:00:00"
}
```

### 8.8 下线内容
**请求方式**: `PUT`
**接口地址**: `/api/rich-content/{id}/unpublish`
**是否需要认证**: 是

**功能描述**: 下线指定的富文本内容

**路径参数:**
- `id` (Long): 内容ID

### 8.9 设置内容为置顶
**请求方式**: `PUT`
**接口地址**: `/api/rich-content/{id}/top`
**是否需要认证**: 是

**功能描述**: 将指定内容设置为置顶

**路径参数:**
- `id` (Long): 内容ID

### 8.10 取消内容置顶
**请求方式**: `DELETE`
**接口地址**: `/api/rich-content/{id}/top`
**是否需要认证**: 是

**功能描述**: 取消指定内容的置顶状态

**路径参数:**
- `id` (Long): 内容ID

### 8.11 设置内容为推荐
**请求方式**: `PUT`
**接口地址**: `/api/rich-content/{id}/featured`
**是否需要认证**: 是

**功能描述**: 将指定内容设置为推荐

**路径参数:**
- `id` (Long): 内容ID

### 8.12 取消内容推荐
**请求方式**: `DELETE`
**接口地址**: `/api/rich-content/{id}/featured`
**是否需要认证**: 是

**功能描述**: 取消指定内容的推荐状态

**路径参数:**
- `id` (Long): 内容ID

### 8.13 增加浏览次数
**请求方式**: `PUT`
**接口地址**: `/api/rich-content/{id}/view`
**是否需要认证**: 否

**功能描述**: 增加指定内容的浏览次数

**路径参数:**
- `id` (Long): 内容ID

### 8.14 增加点赞次数
**请求方式**: `PUT`
**接口地址**: `/api/rich-content/{id}/like`
**是否需要认证**: 否

**功能描述**: 增加指定内容的点赞次数

**路径参数:**
- `id` (Long): 内容ID

### 8.15 增加评论次数
**请求方式**: `PUT`
**接口地址**: `/api/rich-content/{id}/comment`
**是否需要认证**: 否

**功能描述**: 增加指定内容的评论次数

**路径参数:**
- `id` (Long): 内容ID

### 8.16 获取已发布的内容列表
**请求方式**: `GET`
**接口地址**: `/api/rich-content/published`
**是否需要认证**: 是

**功能描述**: 获取所有已发布的内容列表

### 8.17 根据内容类型获取内容列表
**请求方式**: `GET`
**接口地址**: `/api/rich-content/type/{contentType}`
**是否需要认证**: 是

**功能描述**: 根据内容类型获取内容列表

**路径参数:**
- `contentType` (String): 内容类型

### 8.18 根据类型分页获取内容列表
**请求方式**: `GET`
**接口地址**: `/api/rich-content/type/{contentType}/page`
**是否需要认证**: 是

**功能描述**: 根据内容类型分页获取内容列表

**路径参数:**
- `contentType` (String): 内容类型

**查询参数:**
- `page` (Integer): 页码 (默认0)
- `size` (Integer): 每页大小 (默认10)

### 8.19 根据状态获取内容列表
**请求方式**: `GET`
**接口地址**: `/api/rich-content/status/{status}`
**是否需要认证**: 是

**功能描述**: 根据状态获取内容列表

**路径参数:**
- `status` (Integer): 内容状态

### 8.20 获取置顶内容列表
**请求方式**: `GET`
**接口地址**: `/api/rich-content/top`
**是否需要认证**: 是

**功能描述**: 获取置顶内容列表

**查询参数:**
- `status` (Integer): 内容状态 (默认1)

**请求示例:**
```bash
curl -X GET "http://localhost:33380/api/rich-content/top?status=1" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "title": "置顶新闻标题",
      "contentType": "NEWS",
      "author": "张三",
      "isTop": true,
      "publishTime": "2023-12-01T10:00:00"
    }
  ],
  "timestamp": "2023-12-01T10:00:00"
}
```

### 8.21 获取推荐内容列表
**请求方式**: `GET`
**接口地址**: `/api/rich-content/featured`
**是否需要认证**: 是

**功能描述**: 获取推荐内容列表

**查询参数:**
- `status` (Integer): 内容状态 (默认1)

**请求示例:**
```bash
curl -X GET "http://localhost:33380/api/rich-content/featured?status=1" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 2,
      "title": "推荐新闻标题",
      "contentType": "NEWS",
      "author": "李四",
      "isFeatured": true,
      "publishTime": "2023-12-01T10:00:00"
    }
  ],
  "timestamp": "2023-12-01T10:00:00"
}
```

### 8.22 搜索内容
**请求方式**: `GET`
**接口地址**: `/api/rich-content/search`
**是否需要认证**: 是

**功能描述**: 根据关键词搜索内容

**查询参数:**
- `keyword` (String): 搜索关键词
- `status` (Integer): 内容状态 (可选)

### 8.23 根据标签查找内容
**请求方式**: `GET`
**接口地址**: `/api/rich-content/tag/{tag}`
**是否需要认证**: 是

**功能描述**: 根据标签查找内容

**路径参数:**
- `tag` (String): 标签名称

**查询参数:**
- `status` (Integer): 内容状态 (可选)

### 8.24 根据作者查找内容
**请求方式**: `GET`
**接口地址**: `/api/rich-content/author/{author}`
**是否需要认证**: 是

**功能描述**: 根据作者查找内容

**路径参数:**
- `author` (String): 作者名称

**查询参数:**
- `status` (Integer): 内容状态 (可选)

### 8.25 根据关联信息查找内容
**请求方式**: `GET`
**接口地址**: `/api/rich-content/related`
**是否需要认证**: 是

**功能描述**: 根据关联信息查找内容

**查询参数:**
- `relatedId` (Long): 关联ID
- `relatedType` (String): 关联类型

### 8.26 获取最近发布的内容
**请求方式**: `GET`
**接口地址**: `/api/rich-content/recent`
**是否需要认证**: 是

**功能描述**: 获取最近发布的内容

**查询参数:**
- `limit` (Integer): 限制数量 (默认10)
- `status` (Integer): 内容状态 (可选)

### 8.27 获取热门内容
**请求方式**: `GET`
**接口地址**: `/api/rich-content/popular`
**是否需要认证**: 是

**功能描述**: 获取热门内容

**查询参数:**
- `limit` (Integer): 限制数量 (默认10)
- `status` (Integer): 内容状态 (可选)

### 8.28 根据发布时间范围查找内容
**请求方式**: `GET`
**接口地址**: `/api/rich-content/date-range`
**是否需要认证**: 是

**功能描述**: 根据发布时间范围查找内容

**查询参数:**
- `startTime` (String): 开始时间 (格式: yyyy-MM-dd HH:mm:ss)
- `endTime` (String): 结束时间 (格式: yyyy-MM-dd HH:mm:ss)
- `status` (Integer): 内容状态 (可选)

### 8.29 检查标题是否存在
**请求方式**: `GET`
**接口地址**: `/api/rich-content/check-title/{title}`
**是否需要认证**: 是

**功能描述**: 检查标题是否已存在

**路径参数:**
- `title` (String): 内容标题

### 8.30 获取内容统计信息
**请求方式**: `GET`
**接口地址**: `/api/rich-content/statistics`
**是否需要认证**: 是

**功能描述**: 获取内容统计信息

### 8.31 批量操作内容
#### 8.31.1 批量删除内容
**请求方式**: `DELETE`
**接口地址**: `/api/rich-content/batch`
**是否需要认证**: 是

**功能描述**: 批量删除内容

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X DELETE http://localhost:33380/api/rich-content/batch \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

#### 8.31.2 批量发布内容
**请求方式**: `PUT`
**接口地址**: `/api/rich-content/batch/publish`
**是否需要认证**: 是

**功能描述**: 批量发布内容

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/rich-content/batch/publish \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

#### 8.31.3 批量下线内容
**请求方式**: `PUT`
**接口地址**: `/api/rich-content/batch/unpublish`
**是否需要认证**: 是

**功能描述**: 批量下线内容

**请求参数:**
```json
[1, 2, 3]
```

**请求示例:**
```bash
curl -X PUT http://localhost:33380/api/rich-content/batch/unpublish \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/json" \
  -d '[1, 2, 3]'
```

---

## 9. 文件上传接口

### 9.1 上传公司Logo

**接口地址**: `POST /api/admin/upload/logo`

**功能描述**: 上传公司Logo图片

**是否需要认证**: 是

**Content-Type**: `multipart/form-data`

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | MultipartFile | 是 | 图片文件，支持jpg、png格式，最大5MB |

**请求示例**:
```http
POST /api/admin/upload/logo
Authorization: Bearer {JWT_TOKEN}
Content-Type: multipart/form-data

file: [binary data]
```

```bash
curl -X POST http://localhost:33380/api/admin/upload/logo \
  -H "Authorization: Bearer your-jwt-token" \
  -F "file=@/path/to/logo.png"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "fileUrl": "https://oss.example.com/logo/20231201/logo_1701388800000.png",
    "fileName": "logo_1701388800000.png",
    "fileSize": 102400,
    "contentType": "image/png"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

---

### 9.2 上传企业视频

**接口地址**: `POST /api/admin/upload/video`

**功能描述**: 上传企业宣传视频

**是否需要认证**: 是

**Content-Type**: `multipart/form-data`

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | MultipartFile | 是 | 视频文件，支持mp4、avi格式，最大100MB |

**请求示例**:
```http
POST /api/admin/upload/video
Authorization: Bearer {JWT_TOKEN}
Content-Type: multipart/form-data

file: [binary data]
```

```bash
curl -X POST http://localhost:33380/api/admin/upload/video \
  -H "Authorization: Bearer your-jwt-token" \
  -F "file=@/path/to/company-video.mp4"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "fileUrl": "https://oss.example.com/video/20231201/video_1701388800000.mp4",
    "fileName": "video_1701388800000.mp4",
    "originalName": "company-video.mp4",
    "fileSize": 52428800,
    "contentType": "video/mp4",
    "duration": 120,
    "resolution": "1920x1080"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

---

### 9.3 增强图片上传

**接口地址**: `POST /api/admin/upload/image/enhanced`

**功能描述**: 增强版图片上传，支持图片处理和水印

**是否需要认证**: 是

**Content-Type**: `multipart/form-data`

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| file | MultipartFile | 是 | - | 图片文件 |
| category | String | 是 | - | 文件分类 |
| maxSize | Long | 否 | 5242880 | 最大文件大小 (字节) |
| width | Integer | 否 | - | 目标宽度 (可选) |
| height | Integer | 否 | - | 目标高度 (可选) |
| watermark | Boolean | 否 | false | 是否添加水印 |
| description | String | 否 | - | 文件描述 |
| tags | String | 否 | - | 文件标签，逗号分隔 |
| relatedId | Long | 否 | - | 关联ID |
| relatedType | String | 否 | - | 关联类型 |

**请求示例**:
```http
POST /api/admin/upload/image/enhanced
Authorization: Bearer {JWT_TOKEN}
Content-Type: multipart/form-data

file: [binary data]
category: product
maxSize: 5242880
width: 800
height: 600
watermark: true
description: 产品图片
tags: 产品,展示
```

```bash
curl -X POST http://localhost:33380/api/admin/upload/image/enhanced \
  -H "Authorization: Bearer your-jwt-token" \
  -F "file=@/path/to/image.jpg" \
  -F "category=product" \
  -F "maxSize=5242880" \
  -F "width=800" \
  -F "height=600" \
  -F "watermark=true" \
  -F "description=产品图片" \
  -F "tags=产品,展示"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "fileUrl": "https://oss.example.com/product/20231201/image_1701388800000.jpg",
    "fileName": "image_1701388800000.jpg",
    "originalName": "image.jpg",
    "fileSize": 204800,
    "contentType": "image/jpeg",
    "width": 800,
    "height": 600,
    "category": "product",
    "description": "产品图片",
    "tags": ["产品", "展示"],
    "hasWatermark": true
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 9.4 增强视频上传

**接口地址**: `POST /api/admin/upload/video/enhanced`

**功能描述**: 增强版视频上传，支持视频处理和缩略图生成

**是否需要认证**: 是

**Content-Type**: `multipart/form-data`

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| file | MultipartFile | 是 | - | 视频文件 |
| category | String | 是 | - | 文件分类 |
| maxSize | Long | 否 | 104857600 | 最大文件大小 (字节) |
| thumbnail | Boolean | 否 | false | 是否生成缩略图 |
| description | String | 否 | - | 文件描述 |
| tags | String | 否 | - | 文件标签，逗号分隔 |
| relatedId | Long | 否 | - | 关联ID |
| relatedType | String | 否 | - | 关联类型 |

**请求示例**:
```http
POST /api/admin/upload/video/enhanced
Authorization: Bearer {JWT_TOKEN}
Content-Type: multipart/form-data

file: [binary data]
category: corporate
thumbnail: true
description: 企业宣传视频
```

```bash
curl -X POST http://localhost:33380/api/admin/upload/video/enhanced \
  -H "Authorization: Bearer your-jwt-token" \
  -F "file=@/path/to/corporate-video.mp4" \
  -F "category=corporate" \
  -F "thumbnail=true" \
  -F "description=企业宣传视频"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "fileUrl": "https://oss.example.com/corporate/20231201/video_1701388800000.mp4",
    "fileName": "video_1701388800000.mp4",
    "originalName": "corporate-video.mp4",
    "fileSize": 52428800,
    "contentType": "video/mp4",
    "duration": 180,
    "resolution": "1920x1080",
    "category": "corporate",
    "description": "企业宣传视频",
    "thumbnailUrl": "https://oss.example.com/corporate/20231201/video_1701388800000_thumb.jpg"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 9.5 批量上传图片
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/images/batch`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**请求参数:**
- `files` (MultipartFile[]): 图片文件数组
- `category` (String): 文件分类

**请求示例:**
```bash
curl -X POST http://localhost:33380/api/admin/upload/images/batch \
  -H "Authorization: Bearer your-jwt-token" \
  -F "files=@/path/to/image1.jpg" \
  -F "files=@/path/to/image2.jpg" \
  -F "files=@/path/to/image3.jpg" \
  -F "category=product"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "批量上传成功",
  "data": {
    "totalCount": 3,
    "successCount": 3,
    "failedCount": 0,
    "successfulUploads": [
      {
        "fileName": "image1_1701388800000.jpg",
        "fileUrl": "https://oss.example.com/product/20231201/image1_1701388800000.jpg",
        "fileSize": 204800
      }
    ],
    "failedUploads": []
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 7.6 批量上传视频
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/videos/batch`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**请求参数:**
- `files` (MultipartFile[]): 视频文件数组
- `category` (String): 文件分类

### 7.7 获取文件信息
**请求方式**: `GET`
**接口地址**: `/api/admin/upload/file/info`
**是否需要认证**: 是

**查询参数:**
- `fileUrl` (String): 文件URL

**请求示例:**
```bash
curl -X GET "http://localhost:33380/api/admin/upload/file/info?fileUrl=https://oss.example.com/image.jpg" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "fileName": "image.jpg",
    "fileUrl": "https://oss.example.com/image.jpg",
    "fileSize": 102400,
    "contentType": "image/jpeg",
    "uploadTime": "2023-12-01T10:00:00",
    "category": "product",
    "exists": true
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 7.8 检查文件是否存在
**请求方式**: `GET`
**接口地址**: `/api/admin/upload/file/exists`
**是否需要认证**: 是

**查询参数:**
- `fileUrl` (String): 文件URL

**请求示例:**
```bash
curl -X GET "http://localhost:33380/api/admin/upload/file/exists?fileUrl=https://oss.example.com/image.jpg" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "检查完成",
  "data": true,
  "timestamp": "2023-12-01T10:00:00"
}
```

### 7.9 获取预签名上传URL
**请求方式**: `GET`
**接口地址**: `/api/admin/upload/upload/presigned-url`
**是否需要认证**: 是

**查询参数:**
- `fileName` (String): 文件名
- `category` (String): 文件分类
- `expires` (Integer): 过期时间 (秒，默认3600)

**请求示例:**
```bash
curl -X GET "http://localhost:33380/api/admin/upload/upload/presigned-url?fileName=document.pdf&category=document&expires=7200" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": "https://oss.example.com/presigned-upload-url?expires=1701422400&signature=xxx",
  "timestamp": "2023-12-01T10:00:00"
}
```

### 7.10 复制文件
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/file/copy`
**是否需要认证**: 是

**请求参数:**
- `fileUrl` (String): 源文件URL
- `targetCategory` (String): 目标分类

**请求示例:**
```bash
curl -X POST http://localhost:33380/api/admin/upload/file/copy \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "fileUrl=https://oss.example.com/source/image.jpg&targetCategory=backup"
```

**响应示例:**
```json
{
  "code": 200,
  "message": "复制成功",
  "data": "https://oss.example.com/backup/image_copy_1701388800000.jpg",
  "timestamp": "2023-12-01T10:00:00"
}
```

### 7.11 移动文件
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/file/move`
**是否需要认证**: 是

**请求参数:**
- `fileUrl` (String): 源文件URL
- `targetCategory` (String): 目标分类

**请求示例:**
```bash
curl -X POST http://localhost:33380/api/admin/upload/file/move \
  -H "Authorization: Bearer your-jwt-token" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "fileUrl=https://oss.example.com/temp/image.jpg&targetCategory=product"
```

### 7.12 上传资质证书
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/qualification`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**请求参数:**
- `file` (MultipartFile): 资质证书文件

### 7.13 上传团队头像
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/avatar`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**请求参数:**
- `file` (MultipartFile): 头像文件

### 7.14 通用文件上传
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/file`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**请求参数:**
- `file` (MultipartFile): 文件
- `category` (String): 文件分类

### 7.15 产品图片相关接口

#### 7.15.1 上传产品主图
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/product/main-image`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**请求参数:**
- `file` (MultipartFile): 产品主图

#### 7.15.2 批量上传产品图片
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/product/images/batch`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**请求参数:**
- `files` (MultipartFile[]): 产品图片数组

#### 7.15.3 上传单个产品图片
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/product/image`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**请求参数:**
- `file` (MultipartFile): 产品图片

#### 7.15.4 删除产品图片
**请求方式**: `DELETE`
**接口地址**: `/api/admin/upload/product/image`
**是否需要认证**: 是

**查询参数:**
- `imageUrl` (String): 图片URL

#### 7.15.5 批量删除产品图片
**请求方式**: `DELETE`
**接口地址**: `/api/admin/upload/product/images/batch`
**是否需要认证**: 是

**请求参数:**
```json
["https://oss.example.com/image1.jpg", "https://oss.example.com/image2.jpg"]
```

### 7.16 富文本编辑器相关接口

#### 7.16.1 上传富文本编辑器图片
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/editor/image`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**请求参数:**
- `file` (MultipartFile): 编辑器图片

**响应示例:**
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "url": "https://oss.example.com/editor/image.jpg",
    "alt": "image.jpg",
    "href": "",
    "title": ""
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

#### 7.16.2 上传富文本编辑器文件
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/editor/file`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**请求参数:**
- `file` (MultipartFile): 编辑器文件

#### 7.16.3 批量上传富文本编辑器图片
**请求方式**: `POST`
**接口地址**: `/api/admin/upload/editor/images/batch`
**是否需要认证**: 是
**Content-Type**: `multipart/form-data`

**请求参数:**
- `files` (MultipartFile[]): 编辑器图片数组

#### 7.16.4 获取富文本编辑器图片信息
**请求方式**: `GET`
**接口地址**: `/api/admin/upload/editor/image/info`
**是否需要认证**: 是

**查询参数:**
- `imageUrl` (String): 图片URL

### 7.17 删除文件
**请求方式**: `DELETE`
**接口地址**: `/api/admin/upload/file`
**是否需要认证**: 是

**查询参数:**
- `fileUrl` (String): 文件URL

**请求示例:**
```bash
curl -X DELETE "http://localhost:33380/api/admin/upload/file?fileUrl=https://oss.example.com/file.jpg" \
  -H "Authorization: Bearer your-jwt-token"
```

---

## 8. 文件访问接口

> ⚠️ **注意**: 以下接口用于访问已上传的文件，支持断点续传和视频流优化。

### 8.1 访问上传的文件

**接口地址**: `GET /api/files/{file_path}`

**接口描述**: 访问已上传的文件，支持Range请求，优化视频播放体验

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file_path | String | 是 | 文件相对路径 |

**功能特性**:
- ✅ 支持HTTP Range请求，实现断点续传
- ✅ 自动设置合适的Content-Type
- ✅ 设置浏览器缓存优化（缓存1年）
- ✅ 支持大文件流式传输
- ✅ 优化视频播放体验

**示例请求**:
```http
GET /api/files/images/2025/11/abc123.jpg HTTP/1.1
Host: localhost:33380
Range: bytes=0-1023
```

**成功响应** (200 - 完整文件):
```http
HTTP/1.1 200 OK
Content-Type: image/jpeg
Content-Length: 2048576
Accept-Ranges: bytes
Cache-Control: public, max-age=31536000
Content-Disposition: inline; filename="abc123.jpg"
```

**成功响应** (206 - 部分内容):
```http
HTTP/1.1 206 Partial Content
Content-Type: image/jpeg
Content-Length: 1024
Accept-Ranges: bytes
Content-Range: bytes 0-1023/2048576
Cache-Control: public, max-age=31536000
```

### 8.2 下载文件

**接口地址**: `GET /api/files/download/{file_path}`

**接口描述**: 强制下载文件，不在浏览器中直接打开

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file_path | String | 是 | 文件相对路径 |

**示例请求**:
```http
GET /api/files/download/documents/contract.pdf HTTP/1.1
Host: localhost:33380
```

**成功响应**:
```http
HTTP/1.1 200 OK
Content-Type: application/pdf
Content-Disposition: attachment; filename="contract.pdf"
```

### 8.3 获取文件信息

**接口地址**: `GET /api/files/info/{file_path}`

**接口描述**: 获取文件的基本信息（不返回文件内容）

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file_path | String | 是 | 文件相对路径 |

**成功响应**:
```json
{
  "code": 200,
  "message": "获取文件信息成功",
  "data": {
    "fileName": "abc123.jpg",
    "filePath": "images/2025/11/abc123.jpg",
    "fileSize": 2048576,
    "lastModified": 1732003200000,
    "contentType": "image/jpeg"
  }
}
```

---

## 9. 操作日志接口

### 9.1 获取最近的操作日志

**请求方式**: `GET`
**接口地址**: `/api/admin/logs/recent`
**是否需要认证**: 是

**查询参数**:

- `limit` (Integer): 获取数量，最大100，默认10

**请求示例**:

```bash
curl -X GET "http://localhost:33380/api/admin/logs/recent?limit=10" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": 123,
      "operationType": "UPLOAD",
      "operationModule": "IMAGE",
      "operationDesc": "上传了公司Logo: logo.png",
      "targetType": "MediaFile",
      "targetId": 456,
      "targetName": "logo.png",
      "userId": 1,
      "username": "admin",
      "ipAddress": "192.168.1.100",
      "userAgent": "Mozilla/5.0...",
      "createTime": "2025-01-29T10:30:00"
    },
    {
      "id": 122,
      "operationType": "UPDATE",
      "operationModule": "PRODUCT",
      "operationDesc": "更新了产品信息: 牛仔裤系列",
      "targetType": "Product",
      "targetId": 10,
      "targetName": "牛仔裤系列",
      "userId": 1,
      "username": "admin",
      "ipAddress": "192.168.1.100",
      "createTime": "2025-01-29T09:15:00"
    }
  ],
  "timestamp": "2025-01-29T10:30:00"
}
```

### 9.2 获取当前用户的操作日志

**请求方式**: `GET`
**接口地址**: `/api/admin/logs/my`
**是否需要认证**: 是

**请求示例**:

```bash
curl -X GET http://localhost:33380/api/admin/logs/my \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": 122,
      "operationType": "UPDATE",
      "operationModule": "PRODUCT",
      "operationDesc": "更新了产品信息: 牛仔裤系列",
      "targetType": "Product",
      "targetId": 10,
      "targetName": "牛仔裤系列",
      "userId": 1,
      "username": "admin",
      "ipAddress": "192.168.1.100",
      "createTime": "2025-01-29T09:15:00"
    }
  ],
  "timestamp": "2025-01-29T10:30:00"
}
```

### 9.3 分页查询操作日志

**请求方式**: `GET`
**接口地址**: `/api/admin/logs/page`
**是否需要认证**: 是

**查询参数**:

- `page` (Integer): 页码，从0开始，默认0
- `size` (Integer): 每页大小，默认20，最大100

**请求示例**:

```bash
curl -X GET "http://localhost:33380/api/admin/logs/page?page=0&size=20" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "content": [...],
    "totalElements": 150,
    "totalPages": 8,
    "currentPage": 0,
    "size": 20,
    "first": true,
    "last": false
  },
  "timestamp": "2025-01-29T10:30:00"
}
```

### 9.4 按操作模块查询日志

**请求方式**: `GET`
**接口地址**: `/api/admin/logs/module/{operationModule}`
**是否需要认证**: 是

**路径参数**:

- `operationModule` (String): 操作模块（IMAGE/VIDEO/PRODUCT/COMPANY/FILE）

**查询参数**:

- `page` (Integer): 页码，默认0
- `size` (Integer): 每页大小，默认20

**请求示例**:

```bash
curl -X GET "http://localhost:33380/api/admin/logs/module/PRODUCT?page=0&size=20" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "content": [...],
    "totalElements": 25,
    "totalPages": 2,
    "operationModule": "PRODUCT"
  },
  "timestamp": "2025-01-29T10:30:00"
}
```

### 9.5 按操作类型查询日志

**请求方式**: `GET`
**接口地址**: `/api/admin/logs/type/{operationType}`
**是否需要认证**: 是

**路径参数**:

- `operationType` (String): 操作类型（UPLOAD/UPDATE/DELETE/CREATE）

**查询参数**:

- `page` (Integer): 页码，默认0
- `size` (Integer): 每页大小，默认20

**请求示例**:

```bash
curl -X GET "http://localhost:33380/api/admin/logs/type/UPLOAD?page=0&size=20" \
  -H "Authorization: Bearer your-jwt-token"
```

### 9.6 按用户名查询日志

**请求方式**: `GET`
**接口地址**: `/api/admin/logs/user/{username}`
**是否需要认证**: 是

**路径参数**:

- `username` (String): 用户名

**查询参数**:

- `page` (Integer): 页码，默认0
- `size` (Integer): 每页大小，默认20

**请求示例**:

```bash
curl -X GET "http://localhost:33380/api/admin/logs/user/admin?page=0&size=20" \
  -H "Authorization: Bearer your-jwt-token"
```

### 9.7 按时间范围查询日志

**请求方式**: `GET`
**接口地址**: `/api/admin/logs/time-range`
**是否需要认证**: 是

**查询参数**:

- `startTime` (String): 开始时间（ISO格式）
- `endTime` (String): 结束时间（ISO格式）
- `page` (Integer): 页码，默认0
- `size` (Integer): 每页大小，默认20

**请求示例**:

```bash
curl -X GET "http://localhost:33380/api/admin/logs/time-range?startTime=2025-01-01T00:00:00&endTime=2025-01-31T23:59:59&page=0&size=20" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "content": [...],
    "totalElements": 85,
    "totalPages": 5,
    "startTime": "2025-01-01T00:00:00",
    "endTime": "2025-01-31T23:59:59"
  },
  "timestamp": "2025-01-29T10:30:00"
}
```

### 9.8 搜索操作日志

**请求方式**: `GET`
**接口地址**: `/api/admin/logs/search`
**是否需要认证**: 是

**查询参数**:

- `keyword` (String): 搜索关键词
- `page` (Integer): 页码，默认0
- `size` (Integer): 每页大小，默认20

**请求示例**:

```bash
curl -X GET "http://localhost:33380/api/admin/logs/search?keyword=logo&page=0&size=20" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "content": [...],
    "totalElements": 12,
    "totalPages": 1,
    "keyword": "logo"
  },
  "timestamp": "2025-01-29T10:30:00"
}
```

### 9.9 获取操作统计信息

**请求方式**: `GET`
**接口地址**: `/api/admin/logs/statistics`
**是否需要认证**: 是

**请求示例**:

```bash
curl -X GET http://localhost:33380/api/admin/logs/statistics \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "operationTypes": {
      "UPLOAD": 45,
      "UPDATE": 32,
      "DELETE": 8,
      "CREATE": 15
    },
    "operationModules": {
      "IMAGE": 25,
      "PRODUCT": 30,
      "COMPANY": 12,
      "VIDEO": 8,
      "FILE": 25
    },
    "recent7DaysCount": 28,
    "recent30DaysCount": 100
  },
  "timestamp": "2025-01-29T10:30:00"
}
```

### 9.10 复合条件查询日志

**请求方式**: `GET`
**接口地址**: `/api/admin/logs/filter`
**是否需要认证**: 是

**查询参数**:

- `operationType` (String): 操作类型（可选）
- `operationModule` (String): 操作模块（可选）
- `username` (String): 用户名（可选）
- `startTime` (String): 开始时间（可选）
- `endTime` (String): 结束时间（可选）
- `keyword` (String): 搜索关键词（可选）
- `page` (Integer): 页码，默认0
- `size` (Integer): 每页大小，默认20

**请求示例**:

```bash
curl -X GET "http://localhost:33380/api/admin/logs/filter?operationType=UPDATE&operationModule=PRODUCT&page=0&size=20" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "content": [...],
    "totalElements": 15,
    "totalPages": 1,
    "filters": {
      "operationType": "UPDATE",
      "operationModule": "PRODUCT",
      "username": null,
      "startTime": null,
      "endTime": null,
      "keyword": null
    }
  },
  "timestamp": "2025-01-29T10:30:00"
}
```

### 9.11 清理过期日志

**请求方式**: `DELETE`
**接口地址**: `/api/admin/logs/cleanup`
**是否需要认证**: 是

**查询参数**:

- `days` (Integer): 保留天数

**请求示例**:

```bash
curl -X DELETE "http://localhost:33380/api/admin/logs/cleanup?days=90" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "清理完成",
  "data": {
    "deletedCount": 150,
    "beforeTime": "2024-10-31T15:45:00",
    "days": 90
  },
  "timestamp": "2025-01-29T10:30:00"
}
```

### 9.12 按文件分类查询（MediaFile查询接口）

**请求方式**: `GET`
**接口地址**: `/api/admin/upload/files/category/{category}`
**是否需要认证**: 是

**路径参数**:

- `category` (String): 文件分类

**请求示例**:

```bash
curl -X GET "http://localhost:33380/api/admin/upload/files/category/home-carousel" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "files": [
      {
        "id": 1,
        "originalName": "banner1.jpg",
        "fileUrl": "https://oss.example.com/banner1.jpg",
        "fileType": "image",
        "category": "home-carousel",
        "fileSize": 1024000,
        "width": 1920,
        "height": 800,
        "status": 1,
        "createTime": "2025-01-29T10:30:00"
      }
    ],
    "category": "home-carousel",
    "totalCount": 1
  },
  "timestamp": "2025-01-29T10:30:00"
}
```

### 9.13 查询单个最新文件（已废弃）

**请求方式**: `GET`
**接口地址**: `/api/admin/upload/file/category/{category}/latest`
**是否需要认证**: 是

**路径参数**:

- `category` (String): 文件分类

**请求示例**:

```bash
curl -X GET "http://localhost:33380/api/admin/upload/file/category/logo/latest" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "id": 1,
    "originalName": "logo.png",
    "fileUrl": "https://oss.example.com/logo.png",
    "fileType": "image",
    "category": "logo",
    "status": 1,
    "createTime": "2025-01-29T10:30:00"
  },
  "timestamp": "2025-01-29T10:30:00"
}
```

### 9.14 刷新文件列表

**请求方式**: `GET`
**接口地址**: `/api/admin/upload/files/refresh`
**是否需要认证**: 是

**查询参数**:
- `category` (String): 文件分类 (可选)

**请求示例**:

```bash
curl -X GET "http://localhost:33380/api/admin/upload/files/refresh?category=logo" \
  -H "Authorization: Bearer your-jwt-token"
```

**响应示例**:

```json
{
  "code": 200,
  "message": "刷新成功",
  "data": {
    "refreshed": true,
    "timestamp": "2025-01-29T10:30:00",
    "files": [
      {
        "id": 1,
        "originalName": "logo.png",
        "fileUrl": "https://oss.example.com/logo.png",
        "fileType": "image",
        "category": "logo"
      }
    ]
  },
  "timestamp": "2025-01-29T10:30:00"
}
```

---

## 10. 仪表盘接口

### 10.1 获取仪表盘统计信息

**接口地址**: `GET /api/dashboard/statistics`

**功能描述**: 获取仪表盘的统计数据，包括图片数量、视频数量、产品数量等

**是否需要认证**: 否

**请求示例**:
```http
GET /api/dashboard/statistics
```

```bash
curl -X GET http://localhost:33380/api/dashboard/statistics
```

**响应示例**:
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "imageCount": 25,
    "videoCount": 5,
    "productCount": 15
  },
  "timestamp": "2025-01-29T10:00:00"
}
```

---

## 11. 公开媒体文件查询接口

### 11.1 根据分类查询媒体文件列表

**接口地址**: `GET /api/public/media/category/{category}`

**功能描述**: 根据分类查询公开的媒体文件列表，无需认证

**是否需要认证**: 否

**路径参数**:
- `category` (String): 文件分类名称

**支持的分类**:
| 分类名称 | 说明 | 用途 |
|---------|------|------|
| `home-carousel` | 首页轮播图 | 展示前端首页顶部轮播 |
| `logo` | 公司Logo | 网站Logo、页面Logo |
| `company-photos` | 公司照片 | 公司环境、办公室照片 |
| `factory-photos` | 工厂照片 | 生产车间、设备照片 |
| `product-photos` | 产品照片 | 产品图片 |
| `product-video` | 产品视频 | 产品演示视频、宣传视频 |
| `company-intro` | 企业宣传视频 | 企业形象展示视频 |
| `qualification` | 资质证书 | 企业资质证明文件 |
| `avatar` | 团队头像 | 团队成员头像图片 |

**请求示例**:
```bash
curl -X GET http://localhost:33380/api/public/media/category/home-carousel
```

```bash
curl -X GET http://localhost:33380/api/public/media/category/logo
```

**响应示例**:
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "files": [
      {
        "id": 1,
        "originalName": "banner1.jpg",
        "storageName": "home-carousel/2025/11/09/ed3140c6283044ca80dd00fc8d5d5c9f.jpg",
        "filePath": "home-carousel/2025/11/09/ed3140c6283044ca80dd00fc8d5d5c9f.jpg",
        "fileUrl": "http://39.97.60.191:33380/api/files/home-carousel/2025/11/09/ed3140c6283044ca80dd00fc8d5d5c9f.jpg",
        "fileType": "image",
        "mimeType": "image/jpeg",
        "category": "home-carousel",
        "fileSize": 232619,
        "fileExtension": ".jpg",
        "width": 1920,
        "height": 800,
        "duration": null,
        "thumbnailUrl": null,
        "description": "用于首页顶部展示",
        "tags": null,
        "bucketName": "local-storage",
        "storageRegion": "local",
        "cdnDomain": null,
        "isPublic": true,
        "isCdnEnabled": false,
        "status": 1,
        "uploadProgress": 100,
        "errorMessage": null,
        "relatedId": null,
        "relatedType": null,
        "uploadedBy": 34,
        "modifiedBy": null,
        "downloadCount": 0,
        "viewCount": 0,
        "lastAccessTime": null,
        "expireTime": null,
        "metadata": null,
        "createTime": "2025-11-09T16:42:35.699",
        "updateTime": "2025-11-09T16:42:35.699"
      }
    ],
    "category": "home-carousel",
    "totalCount": 1
  },
  "timestamp": "2025-11-13T16:00:00"
}
```

---

### 11.2 获取支持的媒体分类列表

**接口地址**: `GET /api/public/media/categories`

**功能描述**: 获取系统支持的所有媒体文件分类列表

**是否需要认证**: 否

**请求示例**:
```bash
curl -X GET http://localhost:33380/api/public/media/categories
```

**响应示例**:
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "categories": {
      "home-carousel": "首页轮播图",
      "qualification": "资质证书",
      "company-photos": "公司照片",
      "company-intro": "企业宣传视频",
      "factory-photos": "工厂照片",
      "logo": "公司Logo",
      "avatar": "团队头像",
      "product-photos": "产品照片",
      "product-video": "产品视频"
    },
    "totalCount": 9
  },
  "timestamp": "2025-11-13T16:00:00"
}
```

---

### 11.3 获取文件信息

**接口地址**: `GET /api/public/media/file/info`

**功能描述**: 获取指定文件的详细信息

**是否需要认证**: 否

**查询参数**:
- `fileUrl` (String): 文件URL

**请求示例**:
```bash
curl -X GET "http://localhost:33380/api/public/media/file/info?fileUrl=http://localhost:33380/api/files/home-carousel/2025/11/09/banner.jpg"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "fileName": "banner.jpg",
    "fileUrl": "http://localhost:33380/api/files/home-carousel/2025/11/09/banner.jpg",
    "fileSize": 232619,
    "contentType": "image/jpeg",
    "uploadTime": "2025-11-09T16:42:35",
    "category": "home-carousel",
    "exists": true
  },
  "timestamp": "2025-11-13T16:00:00"
}
```

---

### 11.4 检查文件是否存在

**接口地址**: `GET /api/public/media/file/exists`

**功能描述**: 检查指定文件是否存在

**是否需要认证**: 否

**查询参数**:
- `fileUrl` (String): 文件URL

**请求示例**:
```bash
curl -X GET "http://localhost:33380/api/public/media/file/exists?fileUrl=http://localhost:33380/api/files/home-carousel/2025/11/09/banner.jpg"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "操作成功",
  "data": true,
  "timestamp": "2025-11-13T16:00:00"
}
```

**说明**:
- 返回 `true` 表示文件存在
- 返回 `false` 表示文件不存在或无法访问

---

## 12. 测试接口

### 12.1 测试删除功能

**接口地址**: `POST /api/test/delete`

**功能描述**: 测试文件删除功能，返回调试信息

**是否需要认证**: 否

**请求参数**:
- `fileUrl` (String): 要测试删除的文件URL

**请求示例**:
```bash
curl -X POST "http://localhost:33380/api/test/delete?fileUrl=https://oss.example.com/test.jpg"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "测试成功",
  "data": {
    "fileUrl": "https://oss.example.com/test.jpg",
    "exists": true,
    "deleted": true,
    "message": "文件删除成功"
  },
  "timestamp": "2025-01-29T10:00:00"
}
```

### 12.2 检查文件状态

**接口地址**: `GET /api/test/file-status`

**功能描述**: 检查指定文件的状态信息

**是否需要认证**: 否

**请求参数**:
- `fileUrl` (String): 要检查的文件URL

**请求示例**:
```bash
curl -X GET "http://localhost:33380/api/test/file-status?fileUrl=https://oss.example.com/test.jpg"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "检查完成",
  "data": {
    "fileUrl": "https://oss.example.com/test.jpg",
    "exists": true,
    "accessible": true,
    "fileSize": 102400,
    "lastModified": "2025-01-29T10:00:00"
  },
  "timestamp": "2025-01-29T10:00:00"
}
```

### 12.3 测试图片上传

**接口地址**: `POST /api/test/upload/image`

**功能描述**: 测试图片上传功能

**是否需要认证**: 否

**Content-Type**: `multipart/form-data`

**请求参数**:
- `file` (MultipartFile): 图片文件

**请求示例**:
```bash
curl -X POST http://localhost:33380/api/test/upload/image \
  -F "file=@/path/to/test-image.jpg"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "success": true,
    "message": "图片上传成功",
    "fileUrl": "https://oss.example.com/test/test-image_1701388800000.jpg",
    "originalFilename": "test-image.jpg",
    "size": 102400
  },
  "timestamp": "2025-01-29T10:00:00"
}
```

### 12.4 测试视频上传

**接口地址**: `POST /api/test/upload/video`

**功能描述**: 测试视频上传功能

**是否需要认证**: 否

**Content-Type**: `multipart/form-data`

**请求参数**:
- `file` (MultipartFile): 视频文件

**请求示例**:
```bash
curl -X POST http://localhost:33380/api/test/upload/video \
  -F "file=@/path/to/test-video.mp4"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "success": true,
    "message": "视频上传成功",
    "fileUrl": "https://oss.example.com/test/test-video_1701388800000.mp4",
    "originalFilename": "test-video.mp4",
    "size": 52428800
  },
  "timestamp": "2025-01-29T10:00:00"
}
```

### 12.5 检查测试文件

**接口地址**: `GET /api/test/upload/check-file`

**功能描述**: 检查测试目录中的指定文件是否存在

**是否需要认证**: 否

**请求参数**:
- `fileName` (String): 文件名

**请求示例**:
```bash
curl -X GET "http://localhost:33380/api/test/upload/check-file?fileName=test.jpg"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "检查完成",
  "data": {
    "success": true,
    "fileName": "test.jpg",
    "exists": true,
    "message": "文件存在"
  },
  "timestamp": "2025-01-29T10:00:00"
}
```

### 12.6 列出测试上传文件

**接口地址**: `GET /api/test/upload/list-uploads`

**功能描述**: 列出测试目录中的所有上传文件

**是否需要认证**: 否

**请求示例**:
```bash
curl -X GET http://localhost:33380/api/test/upload/list-uploads
```

**响应示例**:
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "success": true,
    "message": "查询到3个文件",
    "uploadPath": "/uploads/test",
    "fileCount": 3,
    "files": [
      "test-image.jpg",
      "test-video.mp4",
      "test-document.pdf"
    ]
  },
  "timestamp": "2025-01-29T10:00:00"
}
```

---




## 13. 公开产品接口

> ⚠️ **注意**: 以下接口为公开访问，无需JWT认证，用于前台展示。

### 13.1 获取所有产品

**接口地址**: `GET /api/public/products`

**接口描述**: 分页获取所有产品（不限状态）

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| page | Integer | 否 | 1 | 页码（从1开始） |
| size | Integer | 否 | 20 | 每页数量 |
| category | String | 否 | - | 产品分类过滤 |

**成功响应**:
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "content": [
      {
        "id": 1,
        "name": "优质牛仔裤",
        "productCode": "JEAN001",
        "description": "舒适耐穿的牛仔裤",
        "category": "服装",
        "price": 299.00,
        "marketPrice": 399.00,
        "mainImage": "http://localhost:33380/api/files/product/images/jean001.jpg",
        "productImages": [
          "http://localhost:33380/api/files/product/images/jean001-1.jpg",
          "http://localhost:33380/api/files/product/images/jean001-2.jpg"
        ],
        "specifications": "尺码: S,M,L,XL",
        "stockQuantity": 100,
        "tags": ["牛仔裤", "休闲", "时尚"],
        "status": 1,
        "isFeatured": true,
        "sortOrder": 1,
        "seoKeywords": "牛仔裤,休闲裤",
        "seoDescription": "优质牛仔裤，舒适耐穿",
        "material": "纯棉",
        "fabricType": "牛仔布",
        "fabricWeight": "320gsm",
        "waistStyle": "中腰",
        "jeansStyle": "直筒",
        "patternFit": "修身",
        "style": "休闲",
        "createdAt": "2025-11-14T10:00:00",
        "updatedAt": "2025-11-14T10:00:00"
      }
    ],
    "totalElements": 1,
    "totalPages": 1,
    "first": true,
    "last": true,
    "size": 20,
    "number": 0
  }
}
```

### 13.2 按分类获取产品

**接口地址**: `GET /api/public/products/category/{category}`

**接口描述**: 根据分类分页获取产品

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| category | String | 是 | - | 产品分类 |
| page | Integer | 否 | 1 | 页码（从1开始） |
| size | Integer | 否 | 20 | 每页数量 |

**示例请求**:
```bash
curl -X GET "http://localhost:33380/api/public/products/category/服装?page=1&size=10"
```

### 13.3 获取产品分类列表

**接口地址**: `GET /api/public/products/categories`

**接口描述**: 获取所有有效的产品分类

**是否需要认证**: 否

**成功响应**:
```json
{
  "code": 200,
  "message": "操作成功",
  "data": ["服装", "配饰", "鞋帽", "箱包"]
}
```

### 13.4 搜索产品

**接口地址**: `GET /api/public/products/search`

**接口描述**: 根据关键词搜索产品

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| keyword | String | 是 | - | 搜索关键词 |
| page | Integer | 否 | 1 | 页码（从1开始） |
| size | Integer | 否 | 20 | 每页数量 |

**示例请求**:
```bash
curl -X GET "http://localhost:33380/api/public/products/search?keyword=牛仔裤&page=1&size=10"
```

### 13.5 获取产品详情

**接口地址**: `GET /api/public/products/{id}`

**接口描述**: 根据产品ID获取产品详情

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 产品ID |

**成功响应**: 返回单个产品的详细信息，格式与13.1中的产品对象相同

**错误响应**:
```json
{
  "code": 404,
  "message": "产品不存在或已下架",
  "timestamp": "2025-11-19T10:00:00"
}
```

### 13.6 获取新品产品列表

**接口地址**: `GET /api/public/products/new`

**接口描述**: 获取上架的新品产品列表，仅返回上架状态的新品产品

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| page | Integer | 否 | 1 | 页码（从1开始） |
| size | Integer | 否 | 20 | 每页数量 |

**特性**:
- 仅返回上架状态的新品产品（status=1且isNew=true）
- 按 `sortOrder` 升序排序（数值越小越靠前）
- 相同排序值时按 `createTime` 降序排序（最新创建的在前）

**请求示例**:
```bash
curl -X GET "http://localhost:33380/api/public/products/new?page=1&size=20"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "content": [
      {
        "id": 1,
        "productName": "2024春季新款连衣裙",
        "productCode": "DRESS-2024-001",
        "description": "新款时尚连衣裙，采用优质面料",
        "category": "连衣裙",
        "price": 299.00,
        "marketPrice": 399.00,
        "mainImage": "https://example.com/image1.jpg",
        "productImages": ["https://example.com/image1.jpg", "https://example.com/image2.jpg"],
        "videoPath": "https://example.com/product-video1.mp4",
        "status": 1,
        "isFeatured": false,
        "isNew": true,
        "isMain": false,
        "sortOrder": 1,
        "createTime": "2025-11-21T10:30:00",
        "updateTime": "2025-11-21T10:30:00",
        "tags": ["新品", "时尚"],
        "material": "纯棉",
        "fabricType": "丝绸",
        "style": "优雅"
      }
    ],
    "totalElements": 50,
    "totalPages": 3,
    "size": 20,
    "number": 0,
    "first": true,
    "last": false
  },
  "timestamp": "2025-11-21T10:30:00"
}
```

### 13.7 获取主要产品列表

**接口地址**: `GET /api/public/products/main`

**接口描述**: 获取上架的主要产品列表，仅返回上架状态的主要产品

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| page | Integer | 否 | 1 | 页码（从1开始） |
| size | Integer | 否 | 20 | 每页数量 |

**特性**:
- 仅返回上架状态的主要产品（status=1且isMain=true）
- 按 `sortOrder` 升序排序（数值越小越靠前）
- 相同排序值时按 `createTime` 降序排序（最新创建的在前）

**请求示例**:
```bash
curl -X GET "http://localhost:33380/api/public/products/main?page=1&size=20"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "content": [
      {
        "id": 2,
        "productName": "经典商务西装套装",
        "productCode": "SUIT-2024-001",
        "description": "高端商务西装，适合正式场合",
        "category": "西装",
        "price": 1299.00,
        "marketPrice": 1599.00,
        "mainImage": "https://example.com/suit1.jpg",
        "productImages": ["https://example.com/suit1.jpg", "https://example.com/suit2.jpg"],
        "videoPath": "https://example.com/suit-video1.mp4",
        "status": 1,
        "isFeatured": true,
        "isNew": false,
        "isMain": true,
        "sortOrder": 1,
        "createTime": "2025-11-21T09:20:00",
        "updateTime": "2025-11-21T09:20:00",
        "tags": ["商务", "经典"],
        "material": "羊毛",
        "fabricType": "羊毛面料",
        "style": "正式"
      }
    ],
    "totalElements": 25,
    "totalPages": 2,
    "size": 20,
    "number": 0,
    "first": true,
    "last": false
  },
  "timestamp": "2025-11-21T09:20:00"
}
```

### 13.8 获取热门推荐产品列表

**接口地址**: `GET /api/public/products/featured`

**接口描述**: 获取上架的热门推荐产品列表，仅返回上架状态的热门推荐产品

**是否需要认证**: 否

**请求参数**:
| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| page | Integer | 否 | 1 | 页码（从1开始） |
| size | Integer | 否 | 20 | 每页数量 |

**特性**:
- 仅返回上架状态的热门推荐产品（status=1且isFeatured=true）
- 按 `sortOrder` 升序排序（数值越小越靠前）
- 相同排序值时按 `createTime` 降序排序（最新创建的在前）
- 支持分页，适合前台展示热门推荐

**请求示例**:
```bash
curl -X GET "http://localhost:33380/api/public/products/featured?page=1&size=20"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "content": [
      {
        "id": 166,
        "name": "1234",
        "productCode": "PRD17638343469545163",
        "description": "146",
        "category": "Men",
        "price": 0.00,
        "marketPrice": 0.00,
        "mainImage": "http://39.97.60.191:33380/api/files/product/2025/11/25/0aa6e85756a948ad93939c3acd2ccaee.jpg",
        "productImages": [
          "http://39.97.60.191:33380/api/files/product/2025/11/25/601fcb537d714eb9a6cc5ce610e0c030.jpg",
          "http://39.97.60.191:33380/api/files/product/2025/11/25/0aa6e85756a948ad93939c3acd2ccaee.jpg"
        ],
        "specifications": "",
        "stockQuantity": 0,
        "tags": [],
        "status": 1,
        "isFeatured": true,
        "isNew": false,
        "isMain": false,
        "sortOrder": 0,
        "seoKeywords": "",
        "seoDescription": "",
        "createdAt": "2025-11-23T01:59:03.158",
        "updatedAt": "2025-11-27T18:20:39.027",
        "material": "",
        "fabricType": "丝绸",
        "fabricWeight": "",
        "waistStyle": "",
        "jeansStyle": "",
        "patternFit": "",
        "style": "",
        "craftsmanship": "",
        "weavingMethod": "",
        "washingProcess": "",
        "needleDetectionProcess": "",
        "patternType": "",
        "printingMethod": "",
        "fashionElements": "",
        "logoPosition": "无",
        "supplyType": "OEM加工服务",
        "origin": "Guangdong, China",
        "fastSampling": false,
        "model": "XXL",
        "season": "",
        "salesUnit": "",
        "videoPath": "http://39.97.60.191:33380/api/files/product/2025/11/23/ed96f48c6e734893965d6f06a1ae2608.mp4"
      }
    ],
    "pageable": {
      "sort": {
        "empty": true,
        "sorted": false,
        "unsorted": true
      },
      "offset": 0,
      "pageSize": 20,
      "pageNumber": 0,
      "paged": true,
      "unpaged": false
    },
    "totalPages": 1,
    "totalElements": 7,
    "last": true,
    "number": 0,
    "size": 20,
    "sort": {
      "empty": true,
      "sorted": false,
      "unsorted": true
    },
    "numberOfElements": 7,
    "first": true,
    "empty": false
  },
  "timestamp": "2025-11-27T12:47:30"
}
```

---

## 14. 产品属性管理总结

### 14.1 完整产品属性列表

产品包含**42个完整属性**，涵盖服装行业的所有专业属性：

#### **13.1.1 系统字段（5个）**
| 属性名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| id | Long | 产品ID | 1 |
| createTime | String | 创建时间（ISO格式） | "2025-11-14T10:00:00" |
| updateTime | String | 更新时间（ISO格式） | "2025-11-14T10:00:00" |
| createdBy | Long | 创建者ID | 1 |
| updatedBy | Long | 更新者ID | 1 |

#### **13.1.2 基本信息属性（5个）**
| 属性名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| productName | String | 产品名称(必填)，最大200字符 | "商务休闲牛仔裤" |
| productCode | String | 产品编码，最大100字符 | "JEANS-001" |
| category | String | 产品分类，最大100字符 | "服装" |
| description | String | 产品描述，最大2000字符 | "高品质商务休闲牛仔裤，舒适透气" |
| specifications | String | 产品规格，最大500字符 | "修身版型，经典蓝色" |

#### **13.1.3 媒体资源属性（3个）**
| 属性名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| mainImage | String | 产品主图URL，最大500字符 | "https://oss.example.com/jeans.jpg" |
| productImages | String | 产品图片列表JSON格式 | "[\"img1.jpg\", \"img2.jpg\"]" |
| videoPath | String | 产品视频路径，最大500字符 | "/uploads/jeans-demo.mp4" |

#### **13.1.4 价格库存属性（3个）**
| 属性名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| price | BigDecimal | 产品价格，不能为负数 | 299.00 |
| marketPrice | BigDecimal | 市场价格，不能为负数 | 399.00 |
| stockQuantity | Integer | 库存数量，不能为负数 | 500 |

#### **13.1.5 服装材质属性（3个）**
| 属性名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| material | String | 材质，最大100字符 | "98%棉 2%氨纶" |
| fabricType | String | 面料类型，最大100字符 | "牛仔布" |
| fabricWeight | String | 面料克重，最大50字符 | "320g" |

#### **13.1.6 服装款式属性（4个）**
| 属性名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| waistStyle | String | 腰型，最大50字符 | "中腰" |
| jeansStyle | String | 牛仔裤款式，最大50字符 | "直筒" |
| patternFit | String | 板型，最大50字符 | "修身" |
| style | String | 样式，最大100字符 | "商务休闲" |

#### **13.1.7 工艺制造属性（4个）**
| 属性名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| craftsmanship | String | 工艺，最大200字符 | "水洗做旧工艺" |
| weavingMethod | String | 编织方法，最大100字符 | "斜纹编织" |
| washingProcess | String | 洗水工艺，最大100字符 | "石洗酵洗" |
| needleDetectionProcess | String | 检针工艺，最大50字符 | "磁检" |

#### **13.1.8 图案设计属性（4个）**
| 属性名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| patternType | String | 图案类型，最大100字符 | "纯色" |
| printingMethod | String | 印花方法，最大100字符 | "无" |
| fashionElements | String | 流行元素，最大200字符 | "简约经典，金属扣装饰" |
| logoPosition | String | 徽标位置，最大100字符 | "后腰右侧" |

#### **13.1.9 供应商业属性（3个）**
| 属性名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| supplyType | String | 供应类型，最大50字符 | "现货供应" |
| origin | String | 原产地，最大100字符 | "中国" |
| fastSampling | Boolean | 7天快速打样：false-否 true-是 | true |

#### **13.1.10 规格包装属性（3个）**
| 属性名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| model | String | 型号，最大50字符 | "S/M/L/XL/XXL" |
| season | String | 季节，最大50字符 | "四季" |
| salesUnit | String | 销售单位，最大50字符 | "件" |

#### **13.1.11 其他属性（7个）**
| 属性名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| tags | String[] | 产品标签列表 | ["牛仔裤", "商务", "休闲"] |
| status | Integer | 产品状态：0-下架 1-上架 2-草稿 | 1 |
| isFeatured | Boolean | 是否推荐：false-否 true-是 | true |
| sortOrder | Integer | 排序值，不能为负数 | 10 |
| seoKeywords | String | SEO关键词，最大200字符 | "牛仔裤,商务休闲,修身" |
| seoDescription | String | SEO描述，最大500字符 | "高品质商务休闲牛仔裤" |

### 14.2 产品属性编辑的主要接口

#### 13.2.1 完整属性编辑接口
**`PUT /api/products/{id}`** (5.5节) - **主要属性编辑接口**
- ✅ 支持编辑所有42个产品属性
- ✅ 适用于产品的完整更新和属性编辑
- ✅ 包含服装行业专业属性：材质、腰型、工艺、图案等

**`POST /api/products`** (5.1节) - **产品创建接口**
- ✅ 支持设置所有42个产品属性
- ✅ 创建时即可配置完整的产品信息
- ✅ 支持服装产品全套专业属性的录入

#### 13.2.2 专门的单项属性修改接口
- `PUT /api/products/{id}/stock` (5.14节) - 仅更新库存数量
- `PUT /api/products/{productId}/main-image` (5.20.3节) - 仅设置主图
- `PUT /api/products/{id}/featured` (5.15节) - 仅设置推荐状态
- `DELETE /api/products/{id}/featured` (5.16节) - 仅取消推荐状态

#### 13.2.3 产品状态管理接口
- `PUT /api/products/{id}/activate` (5.10节) - 上架产品
- `PUT /api/products/{id}/deactivate` (5.11节) - 下架产品
- `PUT /api/products/{id}/save-draft` (5.7节) - 保存为草稿
- `PUT /api/products/{id}/publish` (5.8节) - 从草稿发布
- `GET /api/products/status/drafts` (5.9节) - 获取草稿产品列表

#### 13.2.4 产品查询接口（均包含完整属性）
- `GET /api/products/{id}` (5.2节) - 根据ID获取产品详情
- `GET /api/products/code/{productCode}` (5.3节) - 根据编码获取产品详情
- `GET /api/products` (5.4节) - 分页查询产品列表
- `GET /api/products/active` (5.16节) - 获取上架产品列表
- `GET /api/products/featured` (5.17节) - 获取推荐产品列表

### 14.3 产品属性使用场景和最佳实践

#### 13.3.1 创建产品时设置属性
```bash
POST /api/products
# 创建时直接设置所有42个属性，包含服装专业属性
# 包括：材质、腰型、工艺、图案、供应类型、包装等信息
```

#### 13.3.2 编辑产品属性
```bash
PUT /api/products/{id}
# 使用完整的属性编辑接口，一次性更新多个属性
# 支持服装专业属性的完整更新：材质、款式、工艺等
```

#### 13.3.3 单项属性快速更新
```bash
PUT /api/products/{id}/stock?quantity=150    # 快速更新库存
PUT /api/products/{id}/featured               # 快速设置推荐
```

#### 13.3.4 批量操作
```bash
PUT /api/products/batch/activate    # 批量上架
PUT /api/products/batch/save-draft   # 批量保存草稿
PUT /api/products/batch/publish      # 批量发布
```

### 14.4 数据一致性保证

1. **统一的响应格式**：所有产品接口返回包含完整42个属性的产品对象
2. **统一的数据类型**：所有接口使用相同的数据类型和约束
3. **统一的字段名称**：确保前后端字段命名一致
4. **完整的验证规则**：每个字段都有相应的长度和格式约束
5. **服装专业属性**：确保材质、款式、工艺等21个专业属性的一致性

### 14.5 使用建议

1. **创建产品**：使用 `POST /api/products` 一次设置所有42个属性，包括服装专业属性
2. **编辑属性**：优先使用 `PUT /api/products/{id}` 进行完整编辑，支持服装专业属性更新
3. **频繁更新**：对于高频操作（如库存），使用专门的单项接口
4. **状态管理**：使用状态管理接口进行产品生命周期管理
5. **数据查询**：所有查询接口都返回完整的42个产品属性信息
6. **服装行业**：专业属性包含材质、腰型、款式、工艺、图案、供应类型、包装发货等

---

## 15. 使用说明

### 15.1 认证流程

1. **注册用户**: 调用注册接口创建用户账号
2. **用户登录**: 调用登录接口获取JWT Token
3. **使用Token**: 在需要认证的接口中添加请求头 `Authorization: Bearer <token>`

### 15.2 文件上传注意事项

1. **文件大小限制**:
   - 图片文件: 最大5MB
   - 视频文件: 最大100MB
   - 文档文件: 最大10MB

2. **支持的文件类型**:
   - 图片: jpg, jpeg, png, gif, webp
   - 视频: mp4, avi, mov, wmv
   - 文档: pdf, doc, docx, xls, xlsx, ppt, pptx

3. **命名规则**: 上传的文件会自动重命名为 `原文件名_时间戳.扩展名`

### 15.3 分页查询

所有支持分页的接口都使用统一的分页参数：
- `page`: 页码，从0开始
- `size`: 每页大小，默认10，最大100

分页响应格式：
```json
{
  "content": [], // 数据列表
  "totalElements": 100, // 总记录数
  "totalPages": 10, // 总页数
  "size": 10, // 每页大小
  "number": 0 // 当前页码
}
```

### 15.4 状态说明

#### 产品状态 (status)

- `0` - 下架
- `1` - 上架
- `2` - 草稿

#### 内容状态 (status)

- `0` - 草稿
- `1` - 已发布
- `2` - 已下线

#### 公司信息状态 (status)

- `1` - 正常
- `-1` - 已删除

#### 文件状态 (status)

- `1` - 正常
- `0` - 已删除

#### 操作类型 (operationType)

- `UPLOAD` - 上传操作
- `UPDATE` - 更新操作
- `DELETE` - 删除操作
- `CREATE` - 创建操作

#### 操作模块 (operationModule)

- `IMAGE` - 图片相关操作
- `VIDEO` - 视频相关操作
- `PRODUCT` - 产品相关操作
- `COMPANY` - 公司信息相关操作
- `FILE` - 文件相关操作

---

## 16. 环境配置

### 16.1 开发环境
- **基础URL**: `http://localhost:33380`
- **数据库**: H2/MySQL
- **文件存储**: 本地存储

### 16.2 生产环境
- **基础URL**: `https://api.yourdomain.com`
- **数据库**: MySQL

### 16.3 环境变量
```
# JWT配置
JWT_SECRET=your-secret-key
JWT_EXPIRATION=86400

# 文件上传配置
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=jpg,jpeg,png,gif,pdf,doc,docx

```

---

## 17. Postman Collection 配置

### 17.1 环境变量设置

```json
{
  "name": "企业管理后台",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:33380",
      "type": "string"
    },
    {
      "key": "token",
      "value": "",
      "type": "string"
    },
    {
      "key": "username",
      "value": "admin",
      "type": "string"
    },
    {
      "key": "password",
      "value": "123456",
      "type": "string"
    }
  ]
}
```

### 17.2 认证设置

在Authorization标签页中选择：
- **Type**: Bearer Token
- **Token**: `{{token}}`

### 17.3 全局脚本设置

在Tests标签页中添加以下脚本，用于自动保存Token：

```javascript
// 保存登录接口返回的Token
if (pm.response.code === 200 && pm.request.url.path.includes('/auth/login')) {
    const response = pm.response.json();
    if (response.data && response.data.token) {
        pm.collectionVariables.set('token', response.data.token);
    }
}
```

### 17.4 操作日志接口示例

**获取最近10条操作日志**:
```http
GET {{baseUrl}}/api/admin/logs/recent?limit=10
Authorization: Bearer {{token}}
```

**获取操作统计信息**:
```http
GET {{baseUrl}}/api/admin/logs/statistics
Authorization: Bearer {{token}}
```

**按时间范围查询日志**:
```http
GET {{baseUrl}}/api/admin/logs/time-range?startTime=2025-01-01T00:00:00&endTime=2025-01-31T23:59:59
Authorization: Bearer {{token}}
```

---

## 18. 测试流程建议

### 18.1 基本测试流程

1. **健康检查**
   - 调用 `GET /api/alive` 确认服务正常运行
   - 调用 `GET /api/health` 检查各组件状态

2. **用户注册**
   - 调用 `POST /api/auth/register` 注册新用户
   - 检查用户名和邮箱是否已存在

3. **用户登录**
   - 调用 `POST /api/auth/login` 获取JWT Token
   - Token会自动保存到环境变量中

4. **获取用户信息**
   - 调用 `GET /api/auth/user-info` 验证Token有效性

5. **公司信息管理**
   - 获取公司信息
   - 更新公司信息
   - 上传文件（Logo、视频等）

6. **公开接口测试**
   - 测试各种公开接口，验证数据正确性
   - 测试公开媒体文件查询接口：
     ```http
     GET {{baseUrl}}/api/public/media/category/home-carousel
     GET {{baseUrl}}/api/public/media/categories
     GET {{baseUrl}}/api/public/media/file/exists?fileUrl=xxx
     ```

7. **操作日志功能测试**
   - 上传文件验证日志记录
   - 更新产品查看操作记录
   - 查询最近活动日志
   - 测试各种筛选功能

### 18.2 错误处理测试

1. **无效Token测试**
   - 使用错误的Token访问受保护接口
   - 预期返回401错误

2. **参数验证测试**
   - 提交无效的参数格式
   - 预期返回400错误

3. **权限测试**
   - 未登录用户访问管理接口
   - 预期返回401错误

---

## 19. 常见问题

### 19.1 Token过期

当收到401错误时，需要重新调用登录接口获取新的Token。

### 19.2 文件上传限制

- 文件大小限制：150MB
- 支持的图片格式：jpg, jpeg, png, gif
- 支持的文档格式：pdf, doc, docx
- 支持的视频格式：mp4, avi, mov

### 19.3 数据格式

- 所有日期时间格式均为ISO 8601标准
- 文件URL返回完整的访问路径
- 分页参数：page（页码）、size（每页大小）

---

## 20. 错误码说明

### 20.1 通用错误码

| 错误码 | 说明             | 解决方案               |
| ------ | ---------------- | ---------------------- |
| 200    | 操作成功         | -                      |
| 400    | 请求参数错误     | 检查请求参数格式和内容 |
| 401    | 未认证或认证失败 | 重新登录获取有效Token  |
| 403    | 权限不足         | 联系管理员分配相应权限 |
| 404    | 资源不存在       | 确认资源ID是否正确     |
| 409    | 资源冲突         | 检查资源是否已存在     |
| 429    | 请求过于频繁     | 降低请求频率           |
| 500    | 服务器内部错误   | 联系技术支持           |

### 20.2 业务错误码

| 错误码 | 说明               | 解决方案                   |
| ------ | ------------------ | -------------------------- |
| 1001   | 用户名已存在       | 更换用户名                 |
| 1002   | 邮箱已存在         | 更换邮箱地址               |
| 1003   | 密码错误           | 检查密码是否正确           |
| 1004   | 用户不存在         | 确认用户名或注册新账号     |
| 2001   | 产品不存在         | 检查产品ID                 |
| 2002   | 产品编码已存在     | 更换产品编码               |
| 2003   | 库存不足           | 增加库存或选择其他产品     |
| 3001   | 内容不存在         | 检查内容ID                 |
| 3002   | 标题已存在         | 更换内容标题               |
| 4001   | 公司信息不存在     | 联系管理员初始化           |
| 4002   | 公司信息已存在     | 使用更新接口而非创建       |
| 5001   | 文件上传失败       | 检查文件格式和大小         |
| 5002   | 文件类型不支持     | 使用支持的文件格式         |
| 5003   | 文件大小超限       | 压缩文件或选择更小的文件   |
| 6001   | 操作日志不存在     | 检查查询条件               |
| 6002   | 操作记录查询失败   | 检查查询参数               |
| 6003   | 日志清理失败       | 联系技术支持               |
| 7001   | 文件分类不能为空   | 提供有效的分类参数         |
| 7002   | 查询媒体文件失败   | 检查分类名称或联系技术支持 |
| 7003   | 获取分类列表失败   | 联系技术支持               |
| 7004   | 获取文件信息失败   | 检查文件URL或联系技术支持  |
| 7005   | 检查文件存在性失败 | 检查文件URL或联系技术支持  |

------

## 21. 首页背景管理接口

### 21.1 获取所有背景

**接口地址**: `GET /api/homepage/background`

**功能描述**: 获取所有首页背景配置

**是否需要认证**: 是

**请求示例**:
```http
GET /api/homepage/background
Authorization: Bearer <your-jwt-token>
```

```bash
curl -X GET http://localhost:33380/api/homepage/background \
  -H "Authorization: Bearer <your-jwt-token>"
```

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "backgroundType": "IMAGE",
      "title": "首页背景图",
      "description": "公司首页背景图片",
      "imageUrl": "http://localhost:33380/api/files/homepage/background.jpg",
      "videoUrl": null,
      "cssStyle": "background-size: cover; background-position: center;",
      "isActive": true,
      "sortOrder": 1,
      "createTime": "2023-12-01T10:00:00",
      "updateTime": "2023-12-01T10:00:00"
    }
  ],
  "timestamp": "2023-12-01T10:00:00"
}
```

### 21.2 获取当前活跃背景

**接口地址**: `GET /api/homepage/background/active`

**功能描述**: 获取当前正在使用的首页背景

**是否需要认证**: 是

**请求示例**:
```http
GET /api/homepage/background/active
Authorization: Bearer <your-jwt-token>
```

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "backgroundType": "IMAGE",
    "title": "首页背景图",
    "description": "公司首页背景图片",
    "imageUrl": "http://localhost:33380/api/files/homepage/background.jpg",
    "cssStyle": "background-size: cover; background-position: center;",
    "isActive": true,
    "sortOrder": 1
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 21.3 根据类型获取背景

**接口地址**: `GET /api/homepage/background/type/{backgroundType}`

**功能描述**: 根据背景类型获取背景列表

**是否需要认证**: 是

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| backgroundType | String | 是 | 背景类型 (IMAGE/VIDEO/CSS) | "IMAGE" |

**请求示例**:
```http
GET /api/homepage/background/type/IMAGE
Authorization: Bearer <your-jwt-token>
```

### 21.4 根据ID获取背景

**接口地址**: `GET /api/homepage/background/{id}`

**功能描述**: 根据ID获取特定背景信息

**是否需要认证**: 是

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| id | Long | 是 | 背景ID | 1 |

### 21.5 创建新背景

**接口地址**: `POST /api/homepage/background`

**功能描述**: 创建新的首页背景配置

**是否需要认证**: 是

**请求体**:
```json
{
  "backgroundType": "IMAGE",
  "title": "新背景图",
  "description": "新的首页背景图片",
  "imageUrl": "http://localhost:33380/api/files/homepage/new-bg.jpg",
  "cssStyle": "background-size: cover;",
  "sortOrder": 2
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 2,
    "backgroundType": "IMAGE",
    "title": "新背景图",
    "description": "新的首页背景图片",
    "imageUrl": "http://localhost:33380/api/files/homepage/new-bg.jpg",
    "cssStyle": "background-size: cover;",
    "isActive": false,
    "sortOrder": 2,
    "createTime": "2023-12-01T10:00:00"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 21.6 更新背景

**接口地址**: `PUT /api/homepage/background/{id}`

**功能描述**: 更新现有背景配置

**是否需要认证**: 是

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| id | Long | 是 | 背景ID | 1 |

**请求体**:
```json
{
  "title": "更新后的标题",
  "description": "更新后的描述",
  "cssStyle": "background-size: contain; background-position: top;"
}
```

### 21.7 删除背景

**接口地址**: `DELETE /api/homepage/background/{id}`

**功能描述**: 删除指定的背景配置

**是否需要认证**: 是

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| id | Long | 是 | 背景ID | 1 |

### 21.8 切换背景状态

**接口地址**: `PUT /api/homepage/background/{id}/status`

**功能描述**: 启用或禁用背景

**是否需要认证**: 是

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| id | Long | 是 | 背景ID | 1 |

**请求体**:
```json
{
  "isActive": true
}
```

### 21.9 设置为活跃背景

**接口地址**: `PUT /api/homepage/background/{id}/active`

**功能描述**: 将指定背景设置为当前活跃背景

**是否需要认证**: 是

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| id | Long | 是 | 背景ID | 1 |

---

## 22. 公开首页背景接口

### 22.1 获取当前首页背景

**接口地址**: `GET /api/public/homepage/background/current`

**功能描述**: 获取当前正在使用的首页背景（公开接口）

**是否需要认证**: 否

**请求示例**:
```http
GET /api/public/homepage/background/current
```

```bash
curl -X GET http://localhost:33380/api/public/homepage/background/current
```

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "backgroundType": "IMAGE",
    "title": "首页背景图",
    "imageUrl": "http://localhost:33380/api/files/homepage/background.jpg",
    "cssStyle": "background-size: cover; background-position: center;",
    "isActive": true,
    "sortOrder": 1
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 22.2 获取当前背景CSS样式

**接口地址**: `GET /api/public/homepage/background/css`

**功能描述**: 获取当前首页背景的CSS样式（用于前端直接应用）

**是否需要认证**: 否

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "cssStyle": "background-size: cover; background-position: center; background-image: url('http://localhost:33380/api/files/homepage/background.jpg');",
    "backgroundType": "IMAGE"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 22.3 获取所有活跃背景

**接口地址**: `GET /api/public/homepage/background/active`

**功能描述**: 获取所有处于活跃状态的背景

**是否需要认证**: 否

### 22.4 根据类型获取活跃背景

**接口地址**: `GET /api/public/homepage/background/type/{backgroundType}`

**功能描述**: 根据背景类型获取活跃背景列表

**是否需要认证**: 否

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| backgroundType | String | 是 | 背景类型 (IMAGE/VIDEO/CSS) | "IMAGE" |

### 22.5 获取背景配置信息

**接口地址**: `GET /api/public/homepage/background/info`

**功能描述**: 获取首页背景的配置信息

**是否需要认证**: 否

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "hasBackground": true,
    "backgroundType": "IMAGE",
    "hasVideo": false,
    "cssApplied": true,
    "supportsAnimation": false,
    "lastUpdateTime": "2023-12-01T10:00:00"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

---

## 23. 首页图标配置接口

### 23.1 获取首页图标配置

**接口地址**: `GET /api/public/config/home-icons.json`

**功能描述**: 获取首页图标的JSON配置文件

**是否需要认证**: 否

**请求示例**:
```http
GET /api/public/config/home-icons.json
```

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "icons": [
      {
        "id": 1,
        "name": "产品中心",
        "icon": "products",
        "url": "/products",
        "description": "查看所有产品",
        "sortOrder": 1,
        "isActive": true
      },
      {
        "id": 2,
        "name": "关于我们",
        "icon": "about",
        "url": "/about",
        "description": "了解公司信息",
        "sortOrder": 2,
        "isActive": true
      }
    ]
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 23.2 获取首页图标信息

**接口地址**: `GET /api/public/config/home-icons/info`

**功能描述**: 获取首页图标的详细信息

**是否需要认证**: 否

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "totalIcons": 6,
    "activeIcons": 6,
    "iconCategories": ["navigation", "service", "contact"],
    "lastUpdateTime": "2023-12-01T10:00:00",
    "icons": [
      {
        "id": 1,
        "name": "产品中心",
        "iconType": "font-awesome",
        "iconValue": "fa-box",
        "url": "/products",
        "description": "查看所有产品"
      }
    ]
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

---

## 24. 公司联系方式管理接口

### 24.1 获取联系方式信息（管理员）

**接口地址**: `GET /api/company/admin/contact/info`

**功能描述**: 获取公司联系方式信息（管理员接口）

**是否需要认证**: 是

**请求示例**:
```http
GET /api/company/admin/contact/info
Authorization: Bearer <your-jwt-token>
```

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "companyName": "示例科技有限公司",
    "contactPerson": "张三",
    "phone": "+86-13800138000",
    "email": "contact@example.com",
    "address": "北京市朝阳区示例大厦A座1001室",
    "wechat": "example_wechat",
    "qq": "123456789",
    "whatsapp": "+86-13800138000",
    "website": "https://www.example.com",
    "businessHours": "周一至周五 9:00-18:00",
    "createTime": "2023-12-01T10:00:00",
    "updateTime": "2023-12-01T10:00:00"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 24.2 更新联系方式信息

**接口地址**: `PUT /api/company/admin/contact/info`

**功能描述**: 更新公司联系方式信息

**是否需要认证**: 是

**请求体**:
```json
{
  "contactPerson": "李四",
  "phone": "+86-13900139000",
  "email": "newcontact@example.com",
  "address": "北京市朝阳区新大厦B座2001室",
  "wechat": "new_wechat",
  "qq": "987654321",
  "whatsapp": "+86-13900139000",
  "website": "https://www.newsite.com",
  "businessHours": "周一至周五 8:30-17:30"
}
```

### 24.3 获取WhatsApp联系信息

**接口地址**: `GET /api/company/admin/contact/whatsapp`

**功能描述**: 获取WhatsApp联系方式

**是否需要认证**: 是

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "whatsappNumber": "+86-13800138000",
    "whatsappLink": "https://wa.me/8613800138000",
    "qrCodeUrl": "http://localhost:33380/api/files/whatsapp-qr.png",
    "isActive": true,
    "updateTime": "2023-12-01T10:00:00"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 24.4 检查联系方式是否存在

**接口地址**: `GET /api/company/admin/contact/info/check`

**功能描述**: 检查公司联系方式信息是否存在

**是否需要认证**: 是

**响应示例**:
```json
{
  "code": 200,
  "message": "检查完成",
  "data": {
    "exists": true,
    "hasBasicInfo": true,
    "hasWhatsApp": true,
    "isComplete": true
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

---

## 25. 公开联系方式接口

### 25.1 获取联系方式（公开）

**接口地址**: `GET /api/company/public/contact`

**功能描述**: 获取公司联系方式（公开接口）

**是否需要认证**: 否

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "companyName": "示例科技有限公司",
    "contactPerson": "张三",
    "phone": "+86-13800138000",
    "email": "contact@example.com",
    "address": "北京市朝阳区示例大厦A座1001室",
    "wechat": "example_wechat",
    "qq": "123456789",
    "website": "https://www.example.com",
    "businessHours": "周一至周五 9:00-18:00"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 25.2 获取WhatsApp号码（公开）

**接口地址**: `GET /api/company/public/contact/whatsapp`

**功能描述**: 获取WhatsApp联系方式（公开接口）

**是否需要认证**: 否

### 25.3 获取WhatsApp聊天链接（公开）

**接口地址**: `GET /api/company/public/contact/whatsapp/link`

**功能描述**: 获取WhatsApp聊天链接（公开接口）

**是否需要认证**: 否

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "whatsappLink": "https://wa.me/8613800138000?text=您好，我对贵公司的产品很感兴趣",
    "qrCodeUrl": "http://localhost:33380/api/files/whatsapp-qr.png",
    "phoneNumber": "+86-13800138000"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 25.4 获取公司Logo（公开）

**接口地址**: `GET /api/company/public/logo`

**功能描述**: 获取公司Logo URL（公开接口）

**是否需要认证**: 否

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "logoUrl": "http://localhost:33380/api/files/company/logo.png",
    "hasLogo": true,
    "logoSize": {
      "width": 200,
      "height": 100
    }
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 25.5 获取公司Logo图片文件

**接口地址**: `GET /api/company/public/logo/image`

**功能描述**: 直接获取公司Logo图片文件

**是否需要认证**: 否

**请求示例**:
```http
GET /api/company/public/logo/image
```

---

## 26. 产品批量操作接口

### 26.1 批量删除产品

**接口地址**: `DELETE /api/products/batch`

**功能描述**: 批量删除多个产品

**是否需要认证**: 是

**请求体**:
```json
{
  "productIds": [1, 2, 3, 4, 5]
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "批量删除成功",
  "data": {
    "deletedCount": 5,
    "failedIds": []
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

### 26.2 批量激活产品

**接口地址**: `PUT /api/products/batch/activate`

**功能描述**: 批量激活多个产品

**是否需要认证**: 是

**请求体**:
```json
{
  "productIds": [1, 2, 3]
}
```

### 26.3 批量停用产品

**接口地址**: `PUT /api/products/batch/deactivate`

**功能描述**: 批量停用多个产品

**是否需要认证**: 是

### 26.4 批量保存为草稿

**接口地址**: `PUT /api/products/batch/save-draft`

**功能描述**: 批量将产品保存为草稿状态

**是否需要认证**: 是

### 26.5 批量发布产品

**接口地址**: `PUT /api/products/batch/publish`

**功能描述**: 批量发布草稿产品

**是否需要认证**: 是

---

## 27. 富文本内容批量操作接口

### 27.1 批量发布内容

**接口地址**: `PUT /api/rich-content/batch/publish`

**功能描述**: 批量发布多个富文本内容

**是否需要认证**: 是

**请求体**:
```json
{
  "contentIds": [1, 2, 3]
}
```

### 27.2 批量下线内容

**接口地址**: `PUT /api/rich-content/batch/unpublish`

**功能描述**: 批量下线多个富文本内容

**是否需要认证**: 是

### 27.3 批量删除内容

**接口地址**: `DELETE /api/rich-content/batch`

**功能描述**: 批量删除多个富文本内容

**是否需要认证**: 是

---

## 28. 产品库存管理接口

### 28.1 更新产品库存

**接口地址**: `PUT /api/products/{id}/stock`

**功能描述**: 更新指定产品的库存数量

**是否需要认证**: 是

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| id | Long | 是 | 产品ID | 1 |

**请求体**:
```json
{
  "stock": 100,
  "operation": "SET"  // SET, ADD, SUBTRACT
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "库存更新成功",
  "data": {
    "productId": 1,
    "previousStock": 50,
    "currentStock": 100,
    "operation": "SET"
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

---

## 29. 文件复制和移动接口

### 29.1 复制文件

**接口地址**: `POST /api/admin/upload/file/copy`

**功能描述**: 复制文件到目标分类

**是否需要认证**: 是

**请求体**:
```json
{
  "sourceUrl": "http://localhost:33380/api/files/source/image.jpg",
  "targetCategory": "products",
  "newFileName": "copied-image.jpg"
}
```

### 29.2 移动文件

**接口地址**: `POST /api/admin/upload/file/move`

**功能描述**: 移动文件到目标分类

**是否需要认证**: 是

**请求体**:
```json
{
  "sourceUrl": "http://localhost:33380/api/files/source/image.jpg",
  "targetCategory": "products"
}
```

---

## 30. 获取预签名上传URL接口

### 30.1 获取预签名上传URL

**接口地址**: `GET /api/admin/upload/upload/presigned-url`

**功能描述**: 获取用于直接上传文件的预签名URL

**是否需要认证**: 是

**查询参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| fileName | String | 是 | 文件名 | "product.jpg" |
| fileType | String | 是 | 文件类型 | "image/jpeg" |
| category | String | 是 | 文件分类 | "products" |

**响应示例**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "uploadUrl": "https://your-cloud-storage.com/upload?signature=...",
    "fileUrl": "http://localhost:33380/api/files/products/product.jpg",
    "expiresIn": 3600
  },
  "timestamp": "2023-12-01T10:00:00"
}
```

---

## 📋 最终接口统计总结

### 🔧 完整技术更新
- **版本**: v1.5
- **更新日期**: 2025-11-30
- **新增控制器**: 5个
- **新增接口**: 35+ 个

### 📊 最终接口统计
- **总接口数量**: 约 195+ 个REST API接口
- **新增接口**: 35+ 个 (本次更新)
- **需要认证的接口**: 约 135+ 个 (69%)
- **公开访问接口**: 约 60+ 个 (31%)

### 🎯 完整功能模块列表

#### 核心业务模块 (15个主要模块)
1. **系统健康检查** (2个接口)
2. **用户认证与授权** (6个接口)
3. **公司信息管理** (11个接口)
4. **产品管理** (55+ 个接口，包含批量操作)
5. **新品管理** (8个接口)
6. **主要产品管理** (8个接口)
7. **富文本内容管理** (35+ 个接口，包含批量操作)
8. **首页背景管理** (9个接口)
9. **首页图标配置** (2个接口)
10. **公司联系方式管理** (8个接口)
11. **文件上传与管理** (25+ 个接口)
12. **文件访问与下载** (3个接口)
13. **操作日志管理** (13个接口)
14. **仪表盘统计** (1个接口)
15. **公开接口集合** (15+ 个接口)

#### 公开接口分类
- **公开产品接口**: 获取产品信息、分类、搜索
- **公开公司信息**: 公司简介、联系方式、Logo
- **公开媒体文件**: 图片、视频、文档访问
- **公开首页配置**: 背景图、图标配置

#### 管理员专有接口
- **文件操作**: 上传、复制、移动、删除
- **内容管理**: 创建、编辑、发布、下线
- **系统管理**: 日志查询、统计、配置
- **批量操作**: 产品批量处理、内容批量管理

### 🛡️ 安全与权限
- **JWT认证**: 保护敏感操作接口
- **权限分级**: 公开读取 vs 管理员操作
- **操作日志**: 完整的审计跟踪
- **数据验证**: 严格的输入验证和错误处理

### 📱 移动端与现代化特性
- **响应式设计**: 适配各种屏幕尺寸
- **WhatsApp集成**: 一键联系功能
- **富媒体支持**: 图片、视频、文档处理
- **实时状态**: 动态更新和状态管理

### 🔧 开发友好特性
- **统一响应格式**: 一致的API响应结构
- **完整错误码**: 详细的错误分类和处理
- **分页支持**: 标准化的分页查询
- **批量操作**: 高效的批量处理能力
